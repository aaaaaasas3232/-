# 小听启动 - 现代化工程改造 Prompt 集

> 本文件按"步骤"组织。**每一步都是独立的、可回滚的**。
> 你每次**只复制"当前步骤的 Prompt 块"贴给我**（或者整段对话里说"做第 N 步"），不要一次全贴。
> 每一步完成后，我会请你测试，确认 OK 再进入下一步。

---

## 总路线图（你要做到心里有数）

```
第 0 步【本文件开头】        ← 你现在在这里
第 1 步 引入 Vite             拐点：从"双击 HTML"变成"npm run dev"
第 2 步 拆大文件              把一个 JS 拆成多个 JS，代码更清晰
第 3 步 路径别名 + source map 写 @/xxx 代替 ../../../xxx + 报错能定位行号
第 4 步 IndexedDB 分 store    数据库不再"一个坑里塞所有东西"
第 5 步 XSS 防御 + 状态分层   安全性 + 代码不混在一起
第 6 步 选 Vue3 还是 React    【你必须停下来想清楚】，我给对比
第 7 步 原子类（Tailwind）    写样式更快
第 8 步 一键打包              npm run build:single 出单文件 HTML
```

**重要原则：**
1. 每一步我都承诺"做完不破坏现有功能"——做完你测，能跑再下一步。
2. 任何一步你说"卡了/不想做了"，我们停在那一步，原来的代码不会丢（git 提交了）。
3. 你不需要懂每一步的原理，你只需要**复制 prompt + 复制粘贴执行 + 告诉我哪里报错**。

---

## 词汇速查（崩溃版，复制给你以后忘了就翻）

| 术语 | 一句话 | 跟你项目的关系 |
|---|---|---|
| **Vite** | 一个开发工具，让你能用新写法写 JS，还能打包 | 第 1 步装 |
| **路径别名 @/** | 写 `@/js/core.js` 代替 `../../js/core.js` | 第 3 步开 |
| **source map** | 报错时告诉你"哪一行 JS 出错"，不然报错的行号全是打包后的 | 第 3 步开 |
| **store（IndexedDB 的 store）** | 数据库里的"表"，比如 personas 是一个 store，worlds 是另一个 | 第 4 步拆 |
| **XSS** | 用户输入 `<script>偷你密码</script>`，页面真去执行 | 第 5 步挡 |
| **状态分层** | "数据""界面""逻辑"分开放，不搅在一起 | 第 5 步分 |
| **Vue3 prop** | Vue 组件之间传数据的方式（`<MyCard :name="名字">`） | 第 6 步选 |
| **原子类 / Tailwind** | `<div class="p-4 bg-blue-500">` 直接控制样式，不用写 CSS | 第 7 步开 |
| **打包成单文件** | `npm run build:single` 后，整个项目变成一个 index.html，发给朋友双击就能玩 | 第 8 步 |
| **tree-shaking** | 打包时自动删掉没用的代码 | Vite 默认带，你不用配 |
| **编译期** | 代码"打包时"被自动转换的过程 | Vite 默认带 |

---

## 回滚方案

每一步结束我都让你执行：
```bash
git add . && git commit -m "第 N 步完成"
```
万一崩了：
```bash
git log --oneline   # 找上一个 OK 的版本
git reset --hard <commit-id>
```

如果还没初始化 git，第 1 步我会先帮你 `git init`。

---

## 你接下来的动作

1. **读完上面所有内容**（知道我们在干嘛）
2. **告诉我"开始第 1 步"**
3. 我会给你"第 1 步 Prompt 块"，你**整段复制粘贴**发给我
4. 我执行，告诉你结果
5. 你测，OK 后说"第 2 步"
6. 循环

**现在开始等你说"开始第 1 步"。**


---

# ============================================
# 第 1 步 Prompt 块 —— 【已完成 ✅ + 用户已确认跑通】
# ============================================
#
# ✅ 第 1 步完成报告
#
# 创建的文件：
#   - package.json              项目清单
#   - vite.config.js            Vite 配置
#   - .gitignore                忽略 node_modules / dist
#   - index.html                入口 HTML（从 html/index.html 复制并修正路径）
#   - src/main.js               Vite 入口（目前仅 console.log）
#   - html/index.html.bak       原 HTML 备份
#   - js.bak/                   原 js 目录备份
#   - css.bak/                  原 css 目录备份
#
# 修改的文件：
#   - index.html 的 <script> 和 <link> 路径已改为绝对路径 /js/xxx /css/xxx
#
# 现在你可以：
#   npm run dev    → 启动本地服务器（会自动打开浏览器，热更新）
#   npm run build  → 打包到 dist/，152ms 完成
#
# ⚠️ 构建有 5 条 warning：
#   "<script src="/js/db.js"> can't be bundled without type=module"
#   这是因为业务脚本还是老式全局脚本——**这是第 2 步要解决的**。
#   不影响开发模式运行，只是打包时不会压缩这些脚本。
#
# ⚠️ 你现在做的事（不用复制）：
#   1. 打开命令行（PowerShell）
#   2. cd "C:\Users\Administrator\Desktop\小听启动"
#   3. 输入 npm run dev
#   4. 浏览器自动打开 http://localhost:5173
#   5. 看是不是和之前双击 html/index.html 一样
#   6. 如果一样 = 第 1 步成功
#
# 回滚（如果想）：
#   删掉 package.json、vite.config.js、.gitignore、index.html、src/
#   把 html/index.html.bak 改回 html/index.html
#   删掉 node_modules/、dist/
#
# ============================================
#
# 【下一步：你测完 npm run dev 后，回来告诉我"开始第 2 步"】

## ✅ 用户已确认第 1 步跑通

中途遇到：Uncaught ReferenceError: Vue is not defined
原因：删了 <script src="vue.global.prod.js"> 那行
解决：加回来，让 Vite 原样加载全局 Vue 脚本

同时加了 public/favicon.svg 消除 404。

---
# ============================================
# 第 2 步 Prompt 块 —— 【当前】
# ============================================
#
# 🎯 第 2 步（修订版）—— 用 Vue 3 SFC 全量重构 apps.js
#
# 原计划：第 2 步拆 JS、第 3 步别名、第 4 步 IDB、第 5 步 XSS...
# 修订为：跳过中间的硬苦力活，直接到目标架构。
#
# 理由（你定的需求：要最好拓展性）：
#   - apps.js 1411 行迟早要拆成组件，不如一次到位
#   - 你已经在用 Vue 3，换栈成本 = 0
#   - 长期所有需求（队列/状态/DB store/协作）都建立在"组件化"上
#   - 跳过的中间步骤以后补也容易，核心架构错了才难回头
#
# 第 2 步具体做什么：
#   1. 装 @vitejs/plugin-vue（让 Vue 单文件组件 .vue 工作）
#   2. 创建 src/ 骨架（components/, composables/, core/, data/, apps/）
#   3. 把 apps.js 里"可以独立成组件"的代码提为 .vue 文件
#      目标切分（每块独立 SFC）：
#        components/
#          StatusBar.vue       - 顶部时间/信号/电池
#          DynamicIsland.vue   - 灵动岛
#          PhoneCase.vue       - 手机壳 + 屏幕
#          Wallpaper.vue       - 壁纸
#          Desktop.vue         - 桌面图标/文件夹/状态栏
#          AppWindow.vue       - 应用窗口通用壳
#          DockBar.vue         - 底栏
#          Modal.vue           - 弹窗系统
#        composables/
#          usePointer.js       - 长按/拖拽
#          useStatusBar.js     - 状态栏时间
#          useDynamicIsland.js - 灵动岛状态机
#          usePhoneState.js    - 全局手机状态（islandMode 等）
#        core/
#          icons.js            - APP_ICONS, UI_ICONS 等常量
#          templates.js        - islandTemplates, appTemplates
#          actions.js          - createAppAction 等
#          renderers.js        - createDefaultPageRenderer 等
#        apps/
#          registry.js         - 应用注册表（替换 externalAppRegistry）
#          loader.js           - loadExternalApps / loadScript
#          settings.js         - 设置应用（保留）
#          prompt-survey.js    - 提示词调查（保留）
#        data/
#          db.js               - IndexedDB 封装
#          db-stores.js        - store 定义（先占位，第 4 步细化）
#
#   4. main.js 用 ES Module 全量 import
#   5. 保留 window.* 导出作为兼容层（让旧代码不崩），后续逐步去掉
#
# ⚠️ 做的过程中我会做小步验证：
#    每改一个文件 = build 通过 + 页面外观一致
#
# ## ✅ 第 2 步完成（2026-07-11）
#
# 调整：apps.js 实际是"模板工具库"（不是 UI），所以没拆 SFC，拆为 ES Module。
#
# 拆出的文件（共 14 个）：
# - src/core/icons.js         图标常量
# - src/core/escape.js        XSS 防护
# - src/core/actions.js       动作系统
# - src/core/renderers.js     通用渲染器
# - src/core/island-templates.js  灵动岛模板
# - src/core/icon-library.js  图标库 + 设置页构造器
# - src/core/templates.js     appTemplates（7 个内置模板）
# - src/core/page-renderers.js 页面渲染器
# - src/core/island-helper.js 灵动岛助手
# - src/core/store-api.js     数据表 API
# - src/core/app-toolkit.js   App 工具包
# - src/core/app-registry.js  App 注册表
# - src/apps/template-app.js  模板 App
# - src/index.js              Vite 入口（含 window.* 兼容层）
#
# 老 js 文件搬到 public/ 让 Vite 原样 serve：
# - public/js/db.js
# - public/js/core.js
# - public/js/apps/settings.js
# - public/js/apps/prompt-survey.js
# - public/js/vendor/vue.global.prod.js
#
# 结果：
# - npm run build 通过，**0 warning**
# - 主产物 34.36 kB（gzip 11.68 kB）
# - 保留 window.* 兼容层，core.js 无需改动
# - 模板 App 通过 ES Module 注册，行为完全一致
#
# ============================================
