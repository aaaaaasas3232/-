# XSS 防护

## 什么是 XSS

- 全称：`Cross-Site Scripting`（跨站脚本攻击）
- 本质：攻击者把恶意 JS/HTML 注入到页面中，在受害者的浏览器里执行
- 常见后果：偷 cookie、冒充用户操作、篡改页面内容、钓鱼等

## HTML 转义（escapeHtml）

项目 `js/apps.js` 中的转义函数：

```js
function escapeHtml(value) {
    return String(value ?? '')
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#39;');
}
```

### 原理

将特殊字符编码为 HTML 实体：

| 原始字符 | 转义后  | 说明                    |
|----------|---------|-------------------------|
| `&`      | `&amp;` | 防止 & 本身破坏实体     |
| `<`      | `&lt;`  | 防止被当成 HTML 标签    |
| `>`      | `&gt;`  | 防止被当成 HTML 标签    |
| `"`      | `&quot;` | 防止标签属性值被闭合   |
| `'`      | `&#39;` | 防止属性值被单引号闭合  |

### 示例

用户输入：

```html
<script>alert(1)</script>
```

转义后：

```html
&lt;script&gt;alert(1)&lt;/script&gt;
```

浏览器会把它当成纯文本显示，而不会作为脚本执行。

## 适用场景

- 用户输入 / 动态内容插入 HTML 文本节点时
- 例如：评论、昵称、文章内容等直接展示在页面上的场景

## 局限性和补充措施

| 场景                      | escapeHtml 是否足够 | 补充措施                                |
|---------------------------|--------------------|----------------------------------------|
| 插入 HTML 文本节点         | ✅ 足够            |                                        |
| 插入 `<a href="...">`      | ❌ 不够            | 校验 URL 协议，只允许 `http/https`     |
| 插入 `<img src="...">`     | ❌ 不够            | 同上，防止 `javascript:` 伪协议        |
| 插入 `<div onClick="...">` | ❌ 不够            | 绝不能在 onClick 等事件属性中拼接用户输入 |
| 插入 `<style>`             | ❌ 不够            | 使用白名单样式，或严格过滤 `expression`/`javascript:` |
| 插入 JavaScript 字符串     | ❌ 不够            | 需要针对 JS 的转义，如 `\` 转义 `'` `"` |

## 最佳实践

1. **默认转义，非必要不插入 HTML** — 优先使用纯文本展示用户输入
2. **上下文感知转义** — 不同插入位置（HTML 文本、属性、URL、JS）需要不同转义方式
3. **使用成熟库** — 如 OWASP Java Encoder、DOMPurify（前端）等
4. **内容安全策略（CSP）** — 通过 HTTP 响应头限制脚本执行，作为纵深防御
5. **HttpOnly + Secure Cookie** — 即使 XSS 发生，也无法读取 cookie
