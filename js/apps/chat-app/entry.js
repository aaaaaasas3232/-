/**
 * chat-app / 聊天 App 入口
 *
 * ⚠️ 当前是「空骨架」—— 后续逐步把 chat.js 的能力迁进来。
 * 约束:framework 其他文件（index.html / src/index.js / js/apps/index.js）一概不动,
 * 删的时候 rm -rf js/apps/chat-app/ css/chat.css 即可。
 */

import './css.js'; // 副作用 import: 触发 vite 把 chat.css 打包进来
