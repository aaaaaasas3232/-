# Prompt 模块 — 以后做

> 这一节是我 2026-07-30 那次修人设主页 bug 时记下来的"剩下没做"的活，跟 Prompt 模块相关的全放这里。
> 修 bug 时只动了 `defaults.js`、`home-section.js`、`persona.js`（回滚判定），别的东西没碰。

---

## 1. Prompt 模块本体 ⏳

- **现状**：`js/apps/setting/prompt/section.js` 是空占位
- **要做的事**：
  1. IndexedDB `promptTemplates` 表
  2. `settingsSdk.prompts.{list, get, create, update, remove}`
  3. 列表 / 新建 / 编辑 UI 接入 `main.js:81`
- **参考**：文档「人设制作思路 §18」+「App 制作思路 §3.1」都已标 ⏳
- **做完这一项，下面 2 才能开始**

---

## 2. Prompt `{{ai.name}}` 变量名机制

- **现状**：没 prompt 模板库，变量替换没意义
- **前置**：必须先做上面 1
- **倾向语法**：`{{ai.name}}` 双花括号（参考 `worldview.js` 现有风格）
- **跑通后**让 `buildContextFromPersona` 也支持模板模式，用户可在人设主页直接覆盖自动生成

---

## 3. 不同 App 注入不同上下文（Bug #3 消费侧的另一半）

- **现状**：`renderContextBlock` 只是"在预览顶部加一行 `target_app: xxx` 提示"
- **没做的**：不同 App 注入的模块 / 字段可能不一样（产品策略）
- **建议**：跟 Prompt 模块一起设计，不然脱离上下文

---

## 4. 跨 App 数据回流（人设 §11.3 / 11.4）

- **现状**：`memory → timeLine`、`schedule → routines`，目前人设编辑后世界观侧不会同步
- **建议**：设计阶段再聊，是个独立 feature 不是 bug 修
