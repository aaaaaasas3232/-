# 人设制作思路

> 本文档记录「小听启动」项目中**人设（Persona）** 模块的完整设计。
>
> 人设是 AI / 用户的「我是谁 + 我会怎么反应」，**引用世界观**的地点、场所、时间、资产作为上下文。
>
> **核心理念**：先建世界观 → 再建人设 → 人设通过 `settingsSdk` 拉取世界观数据当 prompt。

> **版本**：v2.0（对齐实际代码 v0.19）

---

## 更新日志

| 版本 | 说明 |
|------|------|
| **v2.0** | **本版：完全基于真实代码重写**。清理所有 `toolkit.persona`/`toolkit.social`/`toolkit.world` 为 `settingsSdk.persona`/`settingsSdk`；对齐 `MODULE_KEYS` 实际值（12 个模块）；清理文件结构；移除不存在的 SDK。 |

*最后更新：2026-07-30 v2.0*

---

## 0. 核心定位

**人设（Persona）** 是某个角色的「身份 + 反应模式」，通过 `settingsSdk` **引用**世界观提供的上下文（不复制数据）。

### 0.1 两种类型

| 类型 | 归属 | 说明 |
|------|------|------|
| **AI 人设** | 归属于 AI | AI 在对话中的身份、性格、背景、记忆 |
| **用户人设** | 归属于用户 | 用户在系统中的身份设定 |

### 0.2 三层结构

```
┌────────────────────────────────────────────────────────────────┐
│   人设                                                            │
│   │                                                                │
│   ├── 第1层:本体(必须有)                                          │
│   │   8 个基础字段(姓名/性别/年龄/外貌/性格/bio/经验/头像)       │
│   │   + 绑定世界观 (boundWorldId)                                  │
│   │                                                                │
│   ├── 第2层:动态数据(可选模块,默认关闭)                          │
│   │   12 个模块(见 §3)                                           │
│   │                                                                │
│   └── 第3层:parO 平行卡(待实现)                                  │
│       ├── 基于本体复制                                             │
│       └── 独立运作                                                 │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

---

## 1. 模式:简略版 vs 详细版

| 模式 | 表单 | 字段处理 | 可升级 |
|------|------|---------|--------|
| **简略版** `simple` | 一个大文本框 | 什么都塞里面 | ✅ → 详细版 |
| **详细版** `detailed` | 分字段填写 | 动态数组(可增删改) | ❌ |

---

## 2. 第1层:本体(必须有)

### 2.1 本体字段表

| # | 字段 | 类型 | 必填 | 说明 |
|---|------|------|------|------|
| 1 | `id` | string | ✅ | 系统自动生成 |
| 2 | `type` | string | ✅ | `'ai'` 或 `'user'` |
| 3 | `name` | string | ✅ | 姓名 |
| 4 | `gender` | string | × | 性别 |
| 5 | `age` | number | × | 年龄 |
| 6 | `appearance` | string | × | 外貌 |
| 7 | `personality` | string | × | 性格 |
| 8 | `bio` | string | × | 一句话简介 |
| 9 | `experience` | string | × | 人生经历(纯文本段落) |
| 10 | `avatar` | string | × | 头像(SVG/URL/base64) |
| 11 | `avatarBg` | string | × | 头像背景渐变 |
| 12 | `boundWorldId` | string | × | 绑定的世界观 ID |

### 2.2 本体数据结构

```javascript
// === 详细版 ===
{
    id: 'persona-001',
    type: 'ai',
    mode: 'detailed',

    name: '小黄',
    gender: '男',
    age: 17,
    appearance: '阳光少年,棕色短发',
    personality: '活泼开朗',
    bio: '热爱电竞的高中生',
    experience: '小学接触游戏,初一打排位,高一被青训选中',
    avatar: '<svg>...</svg>',
    avatarBg: 'linear-gradient(145deg, #f59e0b, #ef4444)',
    boundWorldId: 'world-student-life',

    createdAt: Date.now(),
    updatedAt: Date.now()
}
```

---

## 3. 第2层:动态数据(可选模块,默认关闭)

以下 12 个模块都是**独立可选**的，默认关闭，用户根据自己的需要启用。

### 3.1 模块列表

| 模块 | key | 默认 | 说明 |
|------|-----|------|------|
| **偏好** | `preferences` | 关闭 | 爱好/喜欢/讨厌 |
| **习惯** | `habits` | 关闭 | 口癖与小动作 |
| **原生家庭** | `family` | 关闭 | 父母/家庭成员 |
| **社交圈** | `social` | 关闭 | 朋友/敌人/好感度 |
| **作息** | `schedule` | 关闭 | 周/日循环 |
| **节律** | `rhythm` | 关闭 | 生物钟 |
| **心情** | `mood` | 关闭 | 心情预设/权重 |
| **记忆** | `memory` | 关闭 | 重要事件 |
| **世界观** | `worldview` | 关闭 | 世界观描述 |
| **MBTI** | `mbti` | 关闭 | MBTI 性格 |
| **心理** | `psychological` | 关闭 | 心理特征 |
| **道德** | `moral` | 关闭 | 道德观 |
| **技能** | `skills` | 关闭 | 能力技能 |
| **资源** | `resources` | 关闭 | 头像/表情包绑定 |
| **资产说明** | `assetNotes` | 关闭 | 金钱观/经济处境 |
| **promptIds** | `promptIds` | 关闭 | 绑定的 Prompt 组 |

### 3.2 通用字段模式

每个模块都是 `动态数组 + 独立开关`，用户可增删改：

```javascript
{
    enabled: true,              // 是否启用该模块
    injectMode: 'current',      // 是否注入到 AI prompt: 'none' | 'current' | 'full'

    // ... 模块特有字段
}
```

---

## 4. ★ 人生阶段(精简)

**只保留人生阶段**（学校 → 工作 → 退休这种"角色成长节点"）。

### 4.1 数据结构

```javascript
{
    lifePhases: [
        {
            id: 'phase-life-001',
            order: 1,
            name: '小学阶段',
            ageRange: [6, 12],
            tone: '稚嫩',
            description: '还在上学，性格天真活泼',
            mood: '天真好奇'
        },
    ],
    activeLifePhaseId: 'phase-life-001'
}
```

---

## 5. ★ 资源绑定(`boundResources`)

### 5.1 可绑定资源

| 类型 | 说明 |
|------|------|
| `avatarGroupIds` | 头像图组（多个） |
| `stickerGroupIds` | 表情包图组（多个） |
| `apiRefs` | 第三方 API 引用 |
| `promptIds` | Prompt 组绑定 |

### 5.2 数据结构

```javascript
{
    boundResources: {
        avatarGroupIds: ['grp-001'],
        stickerGroupIds: ['grp-002'],
        apiRefs: [{ refType: 'key', refId: 'api-key-001' }],
        promptIds: ['pgrp-001', 'pgrp-002'],
    }
}
```

---

## 6. ★ 资产（v4.1）

### 6.1 数据结构

```javascript
{
    assetBalance: 6000,                          // 当前基础余额
    assetLastSettledAt: 1721270400000,          // 上次结算时间戳
    incomeEvents: [                               // 定时收入事件
        {
            id: 'evt-salary',
            name: '工资',
            amount: 2000,
            frequency: 'monthly',
            dayOfMonth: 20,
            startDate: '2026-01-20',
            enabled: true,
        },
    ],
}
```

### 6.2 资产说明（v4.2）

```javascript
{
    assetNotes: {
        enabled: false,
        description: '出身贫寒\n靠奖学金过活\n对金钱敏感',
    }
}
```

---

## 7. ★ 概率系统

### 7.1 心情概率

```javascript
{
    moodProbability: {
        enabled: true,
        weights: { '开心': 40, '平静': 30, '期待': 10, '低落': 15, '焦虑': 5 }
    }
}
```

---

## 8. 完整人设示例

```javascript
{
    // ===== 元数据 =====
    id: 'persona-001',
    type: 'ai',
    mode: 'detailed',
    createdAt: 1721000000000,
    updatedAt: 1721000000000,

    // ===== 第1层:本体 =====
    name: '小黄',
    gender: '男',
    age: 17,
    appearance: '阳光少年,棕色短发',
    personality: '活泼开朗,有点中二',
    bio: '热爱电竞的高中生',
    experience: '小学接触游戏,初一打排位',
    avatar: '<svg>...</svg>',
    avatarBg: 'linear-gradient(145deg, #f59e0b, #ef4444)',
    boundWorldId: 'world-student-life',

    // ===== 人生阶段 =====
    lifePhases: [
        {
            id: 'phase-life-001',
            order: 1,
            name: '小学阶段',
            ageRange: [6, 12],
            tone: '稚嫩',
            mood: '天真好奇'
        },
    ],
    activeLifePhaseId: 'phase-life-001',

    // ===== 资源绑定 =====
    boundResources: {
        avatarGroupIds: [],
        stickerGroupIds: [],
        apiRefs: [],
        promptIds: [],
    },

    // ===== 资产 =====
    assetBalance: 6000,
    assetLastSettledAt: 1721270400000,
    incomeEvents: [],

    assetNotes: {
        enabled: false,
        description: '',
    },

    // ===== 概率 =====
    moodProbability: {
        enabled: true,
        weights: { '开心': 40, '平静': 30, '期待': 10, '低落': 15, '焦虑': 5 }
    },
}
```

---

## 9. ★ SDK 接口汇总

> **重要**：所有人设 API 通过 `window.settingsSdk` 访问。**不存在 `toolkit.persona`**。

### 9.1 基础 CRUD（通用 Entity API）

```js
// settingsSdk.users / settingsSdk.aiPersons
settingsSdk.users.list()
settingsSdk.users.get(id)
settingsSdk.users.create({ name, type })
settingsSdk.users.update(id, patch)
settingsSdk.users.remove(id)
settingsSdk.users.getActive()
settingsSdk.users.setActive(id)

settingsSdk.aiPersons.list()
settingsSdk.aiPersons.get(id)
settingsSdk.aiPersons.create({ name, type })
settingsSdk.aiPersons.update(id, patch)
settingsSdk.aiPersons.remove(id)
settingsSdk.aiPersons.getActive()
settingsSdk.aiPersons.setActive(id)
```

### 9.2 人设业务（settingsSdk.persona）

来源：`js/apps/setting/world/sdk/persona.js` 的 `bindPersona(sdk)`。

```js
// 模块
settingsSdk.persona.module.list                               // ['preferences', 'schedule', ...]
settingsSdk.persona.module.get(persona, 'preferences')        // 取模块数据
settingsSdk.persona.module.toggle(entityType, id, 'preferences', true)
settingsSdk.persona.module.setInject(entityType, id, 'habits', 'current')
settingsSdk.persona.module.update(entityType, id, moduleKey, patch)

// 人生阶段
settingsSdk.persona.phases.list(persona)
settingsSdk.persona.phases.active(persona)
settingsSdk.persona.phases.add('ai', 'ai0', { name: '大学', ageRange: [18, 22] })
settingsSdk.persona.phases.update('ai', 'ai0', phaseId, { name: '...' })
settingsSdk.persona.phases.remove('ai', 'ai0', phaseId)
settingsSdk.persona.phases.activate('ai', 'ai0', phaseId)

// parO（待实现）
settingsSdk.persona.paro.list(persona)
settingsSdk.persona.paro.clone('ai', 'ai0', { name: '童年版' })
settingsSdk.persona.paro.update('ai', 'ai0', parOId, patch)
settingsSdk.persona.paro.remove('ai', 'ai0', parOId)

// 概率
settingsSdk.persona.probability.roll('ai', 'ai0')
settingsSdk.persona.probability.dailyCalculate('ai', 'ai0')
settingsSdk.persona.probability.setWeights('ai', 'ai0', { '开心': 40, '平静': 30 })
```

### 9.3 日记（settingsSdk.diary）

来源：`js/apps/setting/world/sdk/diary.js`。

```js
settingsSdk.diary.getToday(entityType, entityId)
settingsSdk.diary.get(id)
settingsSdk.diary.upsert(record)
settingsSdk.diary.addSegment(entityType, entityId, date, { text, source })
settingsSdk.diary.updateSegment(entityType, entityId, date, segmentId, { text })
settingsSdk.diary.removeSegment(entityType, entityId, date, segmentId)
settingsSdk.diary.regenerate(entityType, entityId, { generator })
settingsSdk.diary.MOOD_PRESETS         // ['开心','平静',...]
settingsSdk.diary.pickMood(weights)   // 加权抽心情
```

### 9.4 日程（settingsSdk.schedule）

来源：`js/apps/setting/world/sdk/schedule.js`。

```js
settingsSdk.schedule.getForEntity(entityType, entityId)
settingsSdk.schedule.updateForEntity(entityType, entityId, patch)
```

### 9.5 资产桥（toolkit.persona.asset）

来源：`js/apps/setting/persona/persona-bridge.js`。

> **注意**：这是通过 `toolkit.persona.asset` 访问，不是 `settingsSdk`。

```js
// 快照
toolkit.persona.asset.snapshot(entityType, entityId)
// → { balance, accrued, baseBalance, currency: { name, unit }, events: [...] }

// 调整余额
toolkit.persona.asset.adjust(delta, note, entityType, entityId)
// → 新余额

// 设置余额
toolkit.persona.asset.setBalance(value, entityType, entityId)

// 收入事件
toolkit.persona.asset.addIncome(event, entityType, entityId)
toolkit.persona.asset.updateIncome(eventId, patch, entityType, entityId)
toolkit.persona.asset.removeIncome(eventId, entityType, entityId)

// 结算
toolkit.persona.asset.settle(entityType, entityId)
```

---

## 10. 与世界观的关系

**世界观提供类型定义，人设引用 ID 拿数据。**

```
世界观 ─────────► 人设(引用 ID,不复制数据)
地点/场所 ───────► 人设读 places/locations
纪时系统 ───────► 人设读 chronology
资产类型 ───────► 人设读 world.assets
```

---

## 11. UI 结构

```
人设设置页
├── 左侧:人设列表
│     ├── 小黄(AI)
│     └── 小明(用户)
│
└── 右侧:当前人设详情
      ├── 基础信息(8 个本体字段)
      ├── ★ 人生阶段
      ├── ★ 动态模块(12 个,每个有 enabled 开关)
      ├── ★ 资源绑定(头像/表情包/API/Prompt)
      ├── ★ 资产
      └── ★ 概率系统
```

---

## 12. 文件结构

```
js/apps/setting/persona/
├── index.js                      # 注册入口
├── index-ui.js                 # UI 总装配
├── bootstrap.js                # 启动初始化
├── library.js                  # 人设列表/切换逻辑
├── methods.js                  # ★ 人设编辑方法(含资源绑定)
├── home-section.js            # ★ 人设主页 UI
├── home-methods.js            # ★ 人设主页方法
├── renderer.js                # ★ 人设编辑器渲染器
├── income-engine.js           # ★ v4.1 资产计算
├── persona-bridge.js           # ★ toolkit.persona.asset 桥
├── diary-generator.js         # 日记生成器
└── resources-section.js        # ★ 资源绑定 UI(含 Prompt picker)
```

---

## 13. 新增模块三处同步

新增一个动态模块时，**必须三处同步**：

1. **`persona.js` → `MODULE_KEYS`** 白名单

```javascript
const MODULE_KEYS = [
    'preferences', 'schedule', 'rhythm', 'mood', 'memory',
    'worldview', 'mbti', 'psychological', 'moral', 'skills',
    'resources', 'assetNotes',
];
```

2. **`profile-schema.js` → `AI_PERSONA_GROUPS` / `USER_PERSONA_GROUPS`**

3. **`defaults.js` → `DEFAULT_AI_INSTANCE` / `DEFAULT_USER_INSTANCE`**

---

*最后更新：2026-07-30 v2.0（清理 SDK 接口，统一为 settingsSdk.persona）*
