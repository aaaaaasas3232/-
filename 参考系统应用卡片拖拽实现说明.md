# 参考系统应用卡片拖拽实现说明

> 分析对象：`参考/core.js`、`参考/system.js`  
> 对照对象：`js/framework/use-card-mode.js` 及其实际接线代码  
> 分析日期：2026-07-17  
> 本文只分析“打开 App 后，从 Home Indicator 上滑进入应用卡片，再拖动卡片、松手恢复或关闭 App”的逻辑。`参考/core.js` 中桌面图标与 widget 的编辑态拖拽是另一套系统，不属于本文的“应用卡片拖拽”。

---

## 1. 先说结论

当前系统**完全可以做得更像参考系统**，而且不需要退回参考代码的老式写法。

正确方向不是复制 `var`、IIFE、原型继承、`ontouchstart`、逐帧修改 `left/top`，而是：

1. 保留当前系统的 ESM、Vue 组合式函数、Pointer Events、Pointer Capture、`requestAnimationFrame` 节流和 `transform` 合成层。
2. 复刻参考系统清楚的三段式手势结果：
   - 全屏 App 上滑不足阈值：回到全屏。
   - 上滑超过阈值：从手指当前姿态连续过渡到居中的卡片。
   - 卡片拖动不足关闭阈值：回到卡片原始中心，而不是停在松手位置。
   - 卡片拖动超过关闭阈值：向上缩小、淡出并关闭 App。
3. 保留参考系统在卡片态禁用内容交互、用拖拽标志抑制误点击的设计。
4. 修复当前实现中几处已经可以从静态代码确认的状态衔接问题，而不是继续叠加动画参数。

参考系统“手感明确”的主要原因并不是某条神奇缓动曲线，而是它的状态和结果很简单：**拖动时跟手，松手时只有回原位或关闭两种结果，进入卡片时从当前手势姿态连续落位。**

---

## 2. 两个参考文件各自负责什么

### 2.1 `参考/core.js` 才是拖拽系统的实现文件

应用卡片拖拽全部集中在 `BaseApp` 基类中：

- `参考/core.js:2582-2645`：定义 `BaseApp` 与卡片状态。
- `参考/core.js:2784-2857`：创建 App 窗口与 Home Indicator 手势区域。
- `参考/core.js:2885-2955`：创建详情页及详情页 Home Indicator 手势区域。
- `参考/core.js:3065-3134`：详情页 Home Indicator 上滑逻辑。
- `参考/core.js:3136-3194`：打开 App 的初始窗口动画与基础样式。
- `参考/core.js:3196-3268`：主页面 Home Indicator 上滑逻辑。
- `参考/core.js:3270-3316`：从全屏切换到卡片模式。
- `参考/core.js:3318-3351`：从卡片恢复到全屏。
- `参考/core.js:3353-3452`：卡片本体的拖动、透明度、旋转、松手判定。
- `参考/core.js:3454-3515`：卡片飞出、关闭 App、样式与事件清理。
- `参考/core.js:3517-3518`：`EnhancedApp` 只是 `BaseApp` 的兼容别名。
- `参考/core.js:9970-9986`：`PhoneCore.registerApp()` 把 App 实例放入注册表。
- `参考/core.js:10003-10032`：`PhoneCore.openApp()` 最终调用实例的 `open()`。
- `参考/core.js:10189-10207`：将 `BaseApp` / `EnhancedApp` 暴露到 `window`。

因此，真正需要理解和现代化迁移的是 `BaseApp` 这一段，而不是整个一万行左右的 `core.js`。

---

## 3. 参考系统的状态模型

### 3.1 持久窗口状态

`参考/core.js:2609-2615` 在每个 App 实例上保存：

- `appWindow`：真实的 `.app-window` DOM。
- `isCardMode`：当前是不是卡片模式。
- `isCardDragging`：当前手势是否已经越过移动阈值，是否应被视为拖拽。
- `pageStack`：详情页栈，与卡片手势共享同一个外层 `appWindow`。

这里的关键设计是：**主页面、详情页和 Tab 内容都只是 `appWindow` 的内部内容，真正被缩放和拖动的永远是同一个外层窗口。**

### 3.2 Home Indicator 手势的临时状态

`bindHomeIndicatorEvents()` 和 `bindDetailPageHomeIndicator()` 各自通过闭包保存：

- `startY`：按下时纵坐标。
- `moveY`：当前纵向位移。
- `isDragging`：当前 Home Indicator 手势是否有效。

两套函数的计算逻辑完全相同，只是绑定到不同页面中的 `.home-indicator-area`。

### 3.3 卡片拖拽的临时状态

`bindCardDragEvents()` 通过闭包保存：

- `cardStartX` / `cardStartY`：按下卡片时的指针坐标。
- `cardMoveX` / `cardMoveY`：相对按下点的位移。
- `cardStartLeft` / `cardStartTop`：按下前卡片的布局位置。

它不是把卡片永久吸附到任意位置，而是记住“本次手势起点”，松手不足阈值时回到这个起点。

---

## 4. App 窗口和手势区域如何创建

### 4.1 外层窗口

`参考/core.js:2784-2857` 的 `BaseApp.prototype.render()`：

1. 创建 `<div class="app-window hidden">`。
2. 把状态栏间隙、页面栈、Tab Bar、底部区域都放入同一个窗口。
3. 在底部创建 `.home-indicator-area` 和 `.home-indicator`。
4. 把窗口追加到 `#appContainer`。
5. 保存到 `this.appWindow`。
6. 调用 `bindHomeIndicatorEvents()`。
7. 缓存 DOM，下一次打开时复用，不重复创建。

`.home-indicator-area` 是手势热区，`.home-indicator` 只是用户看到的短横条。参考实现把视觉元素与命中区域分开，避免短横条太窄导致难以起手。

### 4.2 详情页仍然操纵同一个外层窗口

`参考/core.js:2885-2955` 创建详情页时，如果允许 Home Indicator，会创建 `.detail-home-indicator` 并调用 `bindDetailPageHomeIndicator()`。

虽然事件从详情页底部开始，但函数中修改的是 `self.appWindow.style`，不是详情页自身。结果是整个 App 连同当前详情页一起缩小成卡片，页面内容和导航状态不会被拆散。

### 4.3 打开 App 时建立全屏初始状态

`参考/core.js:3136-3194` 的 `open()` 会：

1. 确保 `appWindow` 已存在。
2. 计算桌面图标相对手机屏幕的位置，用作 `transform-origin`。
3. 把窗口设为 `left: 0; top: 0; width: 100%; height: 100%`。
4. 从 `scale(0.1)`、`opacity: 0` 动画到 `scale(1)`、`opacity: 1`。
5. 全屏圆角设为 `40px`。
6. 400ms 后清掉临时 transition 与 transform origin。

这建立了后续手势依赖的基础：全屏态从左上角 `0,0` 开始，卡片态再改为固定宽高与居中坐标。

---

## 5. 第一阶段：从 Home Indicator 上滑

主页面逻辑位于 `参考/core.js:3196-3268`，详情页的重复实现位于 `参考/core.js:3065-3134`。

### 5.1 起手

`handleStart(clientY)`：

1. 如果已经是卡片模式，直接返回。
2. 保存 `startY = clientY`。
3. 清空 `moveY`。
4. 设置 `isDragging = true`。
5. 设置 `appWindow.style.transition = 'none'`。

最后一步非常关键：拖动阶段关闭缓动，让 DOM 每次写入立即反映到屏幕，窗口不会被 transition 拖在手指后面。

### 5.2 移动方向限制

移动时：

```text
moveY = currentY - startY
```

如果 `moveY > 0`，说明手指向下，参考系统将其改回 0 并返回。因此这一步只接受上滑，不允许从 Home Indicator 向下拖动窗口。

### 5.3 缩放公式

```text
scale = 1 - abs(moveY) / 800
scale 最低为 0.6
```

示例：

- 上滑 40px：`scale = 0.95`
- 上滑 80px：`scale = 0.90`
- 上滑 120px：`scale = 0.85`

缩放是线性的，参数很朴素，但结果可预测。

### 5.4 位移公式

```text
translateY = moveY × 0.5
```

因为 `moveY` 是负数，窗口向上移动，但只移动手指位移的一半。这制造了“窗口有重量、但仍跟手”的阻尼感。

### 5.5 背景模糊公式

```text
blur = abs(moveY) / 30
blur 最大为 20px
```

拖得越远，桌面越模糊。该值直接写到：

```text
#desktop.style.filter = blur(...px)
```

### 5.6 圆角公式

```text
borderRadius = 40 + abs(moveY) / 15
borderRadius 最大为 50px
```

全屏窗口初始圆角是 40px，上滑时最高增加到 50px。这个变化不大，但会强化“窗口脱离屏幕成为独立物体”的感觉。

### 5.7 每次移动实际写入

拖动阶段只做四类视觉更新：

```text
transform = translateY(moveY × 0.5) scale(scale)
borderRadius = 计算结果
桌面 filter = blur(计算结果)
transition = none
```

没有状态管理库，没有虚拟 DOM，也没有手势速度模型。

### 5.8 松手判定

`handleEnd()` 首先恢复：

```text
transition = all 0.4s cubic-bezier(0.2, 0.8, 0.2, 1)
```

然后只看纵向距离：

```text
abs(moveY) > 120px → switchToCardMode()
否则 → 回到全屏
```

不足阈值时：

- 清空 `transform`。
- 圆角回到 `40px`。
- 桌面 blur 回到 `0px`。

因此上滑不足 120px 的整个过程是可取消的。

### 5.9 触摸与鼠标事件

参考系统分别维护两套入口：

- 触摸：`ontouchstart`、`ontouchmove`、`ontouchend`。
- 鼠标：`onmousedown`，随后在 `document` 上绑定 `mousemove`、`mouseup`。

鼠标监听放在 `document` 上，是为了鼠标离开 Home Indicator 热区后仍能继续收到移动和松手事件。松手后立即移除临时监听。

---

## 6. 第二阶段：从当前手势姿态落到卡片

逻辑位于 `参考/core.js:3270-3316`。

### 6.1 先切状态

```text
isCardMode = true
```

之后 Home Indicator 的移动函数看到卡片态就不会继续改全屏手势姿态。

### 6.2 计算卡片目标位置

固定尺寸：

```text
cardWidth = 280
cardHeight = 400
```

目标位置：

```text
targetX = (screenWidth - 280) / 2
targetY = (screenHeight - 400) / 2 - 30
```

即水平居中，垂直中心再向上 30px。

### 6.3 连续过渡，而不是重新播放另一段固定起点动画

`switchToCardMode()` 没有先清掉 Home Indicator 最后一帧留下的 transform。它直接恢复 0.4s transition，然后修改：

- `left/top`
- `width/height`
- `transform: scale(1)`
- `borderRadius: 25px`
- `boxShadow`

浏览器会从用户松手前的真实状态，例如 `translateY(-60px) scale(0.85)`，连续插值到卡片状态。

这正是参考实现看起来像“同一张窗口被手拖成卡片”的核心。它没有在中间切到一套写死的关键帧起点。

### 6.4 桌面与内容层处理

进入卡片时：

- 桌面 blur 固定到 `20px`。
- App 内容 `pointerEvents = 'none'`。
- Home Indicator 隐藏。
- Tab Bar 隐藏。
- Home Indicator 手势区域隐藏。

禁用内容交互有两个用途：

1. 整张卡片都成为稳定的拖拽表面，内部按钮和滚动区不会抢手势。
2. 卡片模式下不会误触业务按钮。

### 6.5 延迟启用卡片拖拽

参考系统等 400ms 入场动画结束后才：

- 清空临时 transition。
- 调用 `bindCardDragEvents()`。
- 给卡片设置点击恢复全屏逻辑。

这意味着卡片落位动画期间不能立刻被第二次拖拽打断，状态边界比较清楚。

---

## 7. 第三阶段：拖动卡片本体

逻辑位于 `参考/core.js:3353-3452`。

### 7.1 起手

`handleCardStart(clientX, clientY)`：

1. 只在 `isCardMode` 为真时运行。
2. 保存起始指针坐标。
3. 把 `cardMoveX/Y` 清零。
4. 把 `isCardDragging` 设为 false。
5. 从内联 `left/top` 读取卡片本次手势的原始位置。
6. 关闭 transition。

注意：起手不立刻算作拖拽，必须先越过移动阈值。

### 7.2 拖拽启动阈值

```text
abs(cardMoveX) > 5px 或 abs(cardMoveY) > 5px
```

满足任一条件后，`isCardDragging = true`。

这个 5px 阈值用于区分“点卡片恢复全屏”和“拖卡片”。

### 7.3 卡片位置

```text
left = cardStartLeft + cardMoveX
top = cardStartTop + cardMoveY
```

参考系统逐事件修改 `left/top`。行为直观，但会触发布局计算，是它最明显的老式性能问题之一。

### 7.4 拖拽距离

```text
distance = sqrt(cardMoveX² + cardMoveY²)
```

卡片可以朝任意方向拖，透明度和关闭判定都使用二维欧氏距离，而不是只看纵向距离。

### 7.5 透明度

```text
opacity = 1 - distance / 400
opacity 最低为 0.3
```

距离越远越透明，但拖动期间不会完全消失。

### 7.6 旋转

```text
rotate = cardMoveX / 20 度
```

只由横向位移决定：

- 向右拖：顺时针。
- 向左拖：逆时针。
- 纯上下拖：不旋转。

参考实现没有限制最大旋转角度，因此超长横向拖动可能转得过多；这是应现代化修正、而不是照抄的部分。

### 7.7 移动阶段写入

```text
left/top = 原位置 + 位移
opacity = max(0.3, 1 - distance / 400)
transform = rotate(cardMoveX / 20 deg)
transition = none
```

因为位移放在 `left/top`，`transform` 可以单独负责旋转，两者不会覆盖彼此。

---

## 8. 卡片松手：回原位或关闭

逻辑位于 `参考/core.js:3402-3423`。

### 8.1 没越过 5px

如果 `isCardDragging` 仍为 false：

- 清空 transition。
- 不关闭 App。
- 后续 click 事件会把卡片恢复为全屏。

### 8.2 越过 5px 后松手

先设置：

```text
transition = all 0.3s ease-out
```

然后重新计算二维距离。

### 8.3 关闭阈值

```text
distance > 100px → exit()
```

没有速度判定，也没有方向限制。只要卡片中心相对本次按下起点移动超过 100px，就触发关闭。

### 8.4 不足关闭阈值时明确回到原位

参考系统会恢复：

- `left = cardStartLeft`
- `top = cardStartTop`
- `opacity = 1`
- `rotate = 0deg`

这意味着松手不足 100px 时，卡片不会留在新的位置。

### 8.5 防止拖拽后的 click 误恢复全屏

松手后不是立刻清除 `isCardDragging`，而是等待 50ms：

```text
setTimeout(() => isCardDragging = false, 50)
```

因为浏览器可能在拖拽手势结束后继续派发 click。卡片 click 处理器会检查 `!isCardDragging`，这 50ms 的窗口用于吞掉拖拽后产生的 click。

这是简单但有效的“拖拽与点击互斥”设计。

---

## 9. 卡片关闭动画与清理

逻辑位于 `参考/core.js:3454-3515`。

### 9.1 卡片态关闭动画

如果 `isCardMode` 为真：

```text
transform = translateY(-200px) scale(0.5) rotate(0deg)
opacity = 0
transition = all 0.3s ease-out
```

不管卡片最后朝哪个方向拖，参考系统最终都向上飞出。这给所有关闭手势一个统一结果，也更像“把任务卡片向上划掉”。

### 9.2 全屏态关闭动画

如果不是卡片态，则以桌面图标为 transform origin，缩到 `scale(0.1)` 并淡出。它与卡片拖拽不是同一条退出动画。

### 9.3 300ms 后完整复位

动画结束后：

- 给窗口加回 `hidden`，但不销毁 DOM。
- 桌面 blur 回到 0。
- 重置状态栏。
- `isCardMode = false`。
- `isCardDragging = false`。
- 清理 `position/left/top/width/height/transform/opacity/boxShadow`。
- 恢复 Home Indicator、Tab Bar 和内容交互。
- 清除卡片态覆盖的 touch、mouse、click 处理器。

因此下一次打开 App 时不会继承上一次卡片拖拽留下的位置或透明度。

---

## 10. 从卡片点击恢复全屏

逻辑位于 `参考/core.js:3305-3315` 和 `参考/core.js:3318-3351`。

点击卡片时，只有同时满足以下条件才恢复：

```text
isCardMode === true
isCardDragging === false
```

恢复过程：

- `isCardMode = false`
- 0.4s 缓动
- `left/top → 0`
- `width/height → 100%`
- `scale → 1`
- 圆角 → 40px
- 阴影 → none
- 桌面 blur → 0
- 恢复内容、Tab Bar、Home Indicator 交互与显示
- 清除卡片 click 处理器
- 400ms 后清掉临时 transition 与 position

这与进入卡片相反，仍然是同一个 `appWindow` 在做布局插值。

---

## 11. 参考系统完整状态机

```text
CLOSED
  │ open()
  ▼
FULL
  │ Home Indicator 按下
  ▼
INDICATOR_DRAGGING
  ├─ 上滑 ≤ 120px 后松手 ───────────────► FULL（回弹）
  └─ 上滑 > 120px 后松手
         │ switchToCardMode()
         ▼
CARD_ENTERING（400ms，暂不接受卡片拖拽）
         │
         ▼
CARD_IDLE
  ├─ 点击，且未拖拽 ──────────────────► FULL
  └─ 移动超过 5px
         ▼
CARD_DRAGGING
  ├─ 二维距离 ≤ 100px 后松手 ─────────► CARD_IDLE（回原中心）
  └─ 二维距离 > 100px 后松手
         ▼
CARD_DISMISSING（向上 200px、缩到 0.5、淡出）
         │ 300ms
         ▼
CLOSED（清理全部临时样式和事件）
```

参考代码没有显式枚举这些状态，但所有布尔变量、延迟绑定和松手分支组合起来，实际形成了这套状态机。

---

## 12. 为什么参考系统手感容易让人满意

### 12.1 拖动期绝不使用 transition

Home Indicator 与卡片本体起手时都会设 `transition = none`。视觉位置由手势直接决定，不会有“动画追手指”的迟滞。

### 12.2 松手结果非常确定

每段手势只有两种结果：

- Home Indicator：全屏回弹或进入卡片。
- 卡片拖拽：回原位或关闭。

没有“松手后卡片停在任意位置”的第三种模糊状态。

### 12.3 进入卡片从最后一帧手势姿态继续

它不播放一个与手势无关的固定起点 keyframe。浏览器直接从最后的 inline transform 过渡到卡片布局，所以视觉上仍是同一个对象。

### 12.4 卡片态冻结内部交互

内部滚动和按钮不再与拖拽竞争，整张卡片是一个手势表面。

### 12.5 点击与拖拽通过 5px + 50ms 区分

5px 决定是否真的发生拖拽，50ms 标志延迟决定是否吞掉拖后 click。简单，但状态闭环完整。

### 12.6 统一向上飞出

关闭结果不依赖最后拖动方向，用户容易建立肌肉记忆。

---

## 13. 参考实现老旧或不应照抄的部分

### 13.1 IIFE、全局变量和原型继承

`core.js` 把 `BaseApp`、`EnhancedApp` 和 `PhoneCore` 全挂到 `window`；`system.js` 依赖脚本加载顺序。当前项目应继续使用 ESM 和组合式模块。

### 13.2 主页面与详情页重复同一套手势函数

`bindHomeIndicatorEvents()` 与 `bindDetailPageHomeIndicator()` 几乎逐行重复。现代实现应让所有 Home Indicator 共用同一个状态机入口。

### 13.3 触摸与鼠标维护两套事件

参考实现分别写 touch 和 mouse，没有统一 Pointer Events，也没有 Pointer Capture。当前项目的 Pointer Events 方案更可靠。

### 13.4 卡片逐帧修改 `left/top`

这会触发布局与重绘。现代实现应把基准位置固定在中心，拖动位移放进 `translate3d(dx, dy, 0)`，只在模式切换时更新布局尺寸。

### 13.5 没有 `requestAnimationFrame` 节流

触摸移动事件到一次写一次 DOM。在高刷新率设备或复杂 App 内容下可能掉帧。当前的 rAF 合帧值得保留。

### 13.6 `transition: all`

它可能让不需要动画的属性也参与过渡。现代实现应明确列出 transform、opacity、尺寸、圆角和阴影。

### 13.7 旋转角度未限制

`rotate = dx / 20` 在超长横向拖动时可能过度旋转。当前系统限制在 ±18° 更稳妥。

### 13.8 没有 pointer/touch cancel 恢复路径

参考实现未处理 `touchcancel`。当前实现有 `pointercancel` 入口，但恢复逻辑仍需补全。

### 13.9 只按距离，不看速度

快速短甩不会关闭，慢速拖过阈值会关闭。若目标是严格复刻，就保持距离阈值；若后续追求更像真实 iOS，可在稳定后增加速度投影，但不应与本次复刻混在一起。

### 13.10 卡片中心只在进入时计算

窗口尺寸变化后不会自动重算。当前系统的动态测量思路更好，但应保证最终回弹目标始终是最新中心。

---

## 14. 当前系统的实际实现链路

当前卡片系统并不只在 `use-card-mode.js` 一处，完整接线如下：

- `js/framework/utils.js:6-29`：尺寸与阈值常量。
- `js/framework/utils.js:127-149`：Home Indicator 与卡片拖拽状态工厂。
- `js/framework/use-card-mode.js:23-79`：测量手机屏幕与计算卡片中心。
- `js/framework/use-card-mode.js:81-162`：创建状态、计算桌面和 App Shell 样式。
- `js/framework/use-card-mode.js:168-243`：Home Indicator 拖动时的 DOM 直写与桌面效果。
- `js/framework/use-card-mode.js:245-329`：清理、透明度、回位辅助函数与关闭动画。
- `js/framework/use-card-mode.js:332-408`：进入卡片与恢复全屏。
- `js/framework/use-card-mode.js:410-567`：Home Indicator 的 Pointer/Mouse 事件。
- `js/framework/use-card-mode.js:569-734`：卡片本体 Pointer/Mouse 拖拽。
- `js/framework/use-card-mode.js:736-761`：遮罩点击与卡片点击。
- `js/framework/core-shim.js:246-274`：创建 navigation 后注入 `activeApp/appViewMode/closeApp`。
- `js/framework/core-shim.js:466-542`：把卡片状态、样式和事件暴露给 Vue 模板。
- `index.html:242-257`：App Overlay 与 App Shell 的实际绑定。
- `index.html:321-331`：Home Indicator 的实际事件绑定。
- `css/main.css:1256-1316`：全屏/卡片布局、过渡、抓取光标、`touch-action`。
- `css/main.css:1318-1342`：卡片模式禁用内部页面滚动。
- `css/main.css:1344-1374`：定义了进入卡片的 `cardJustEntered` 关键帧，但当前模板没有绑定对应 class，因此这段动画目前未接入实际事件链。

当前系统已经具备参考系统没有的优点：

- Pointer Events 与 Pointer Capture。
- `requestAnimationFrame` 合并 move。
- 拖动时用 `translate3d`，不逐帧改 `left/top`。
- 旋转角度限制。
- `touch-action: none` 与内部滚动冻结。
- ESM、Vue 组合式封装。
- 手机壳与 fullscreen 模式的中心位置测量。

因此没有重写为旧架构的必要。

---

## 15. 当前实现与参考实现逐阶段对照

### 15.1 Home Indicator 起手

相同点：

- 都保存起始坐标。
- 都阻止默认行为。
- 都在拖动期关闭 transition。
- 都只接受向上位移。

当前更好的地方：

- 使用 Pointer Capture，指针离开热区后仍能继续。
- 鼠标兼容路径独立处理，触摸路径可统一走 pointer。

### 15.2 Home Indicator 移动

参考系统：

- 线性缩放。
- 手指位移的一半作为上移。
- 同步增加圆角和 blur。

当前系统：

- 将位移映射到 0～1 进度。
- 对进度使用 ease-out cubic。
- 通过 DOM 直写 `translate3d + scale + borderRadius`。
- 设计目标是从 full 姿态接近最终卡片姿态。

当前思路更现代，但状态同步存在问题，见第 16 节。

### 15.3 进入卡片

参考系统：

- 直接从最后一帧手势 inline style，transition 到卡片目标布局。
- 400ms 后才允许卡片拖拽。

当前系统：

- 切换 `appViewMode = 'card'`。
- 通过 Vue 更新 `left/top/width/height`。
- 维护并暴露 `cardJustEnteredAt`，CSS 也定义了 `card-just-entered` 关键帧，但 `index.html:247-250` 当前没有把该状态绑定为 class，所以这段入场关键帧实际上没有播放。
- `convertToCard()` 恢复 transition 后仍保留 Home Indicator 最后一帧的 inline transform；由于 card 分支不会写回 transform，也没有实际启用的 keyframe 把它动画到 0，当前 transform 所有权存在断点。
- 进入卡片后立即就可以响应卡片 pointerdown。
- 与参考系统“松手后再提交”不同，当前在 `pointermove` 中一达到 100px 就立即调用 `convertToCard()`；这次手势尚未结束，模式已经不可逆地切换。

这是当前与参考手感差异最大的区域。参考系统在松手前始终处于可取消的 `INDICATOR_DRAGGING`；当前系统越过阈值就提前提交，且入场收尾动画没有按源码注释完成接线，因此“拖到阈值附近再收回”和“从最后一帧落到静止卡片”的体验都不会相同。

### 15.4 卡片拖动

视觉公式几乎一致：

- 拖动启动阈值：参考 5px，当前 8px。
- 透明度：两者都是 `max(0.3, 1 - distance / 400)`。
- 旋转：两者都是 `dx / 20`，当前额外限制为 ±18°。
- 关闭距离：两者都是 100px。

实现方式不同：

- 参考：`left/top + rotate`。
- 当前：固定基准 `left/top`，每帧写 `translate3d(dx,dy) rotate(...)`。

当前实现方式性能更好，应保留。

### 15.5 松手不足关闭阈值

参考系统：明确把 `left/top` 回到 `cardStartLeft/cardStartTop`。

当前系统：把响应式 `cardPosition` 更新为 `origin + dx/dy`，再清空 transform。

这会让卡片把松手位置提交成新的基准位置，而不是回到原中心。它与当前代码注释“spring 弹回卡片位”不一致，也与参考系统的行为不同。

### 15.6 超过关闭阈值

参考系统：统一向上 `-200px`，缩到 0.5 并淡出。

当前系统：根据拖动方向计算 `exitX/exitY`，再额外向上偏移 200px，并加入旋转。

如果目标是“更像参考系统”，应改为统一向上飞出；如果喜欢当前的方向感，也可以保留，但它不是参考效果。

### 15.7 卡片点击

参考系统：是否恢复只由 `isCardDragging` 决定，拖后 50ms 再清标志。

当前系统：增加 `suppressNextCardClick`，进入卡片时先设为 true，下一次卡片点击只负责清除标志，不恢复。

当前标志没有按固定时间自动失效，可能导致入场结束后用户第一次真实点击也被吞掉。它比参考系统更容易产生“点了一次没反应”的感觉。

---

## 16. 当前实现中已从代码确认的问题

以下不是主观调参建议，而是当前代码内部可直接验证的不一致。

### 16.1 卡片回弹没有回到中心

位置：`js/framework/use-card-mode.js:696-712`。

不足关闭阈值时，代码计算：

```text
finalX = originX + dx
finalY = originY + dy
cardPosition = { finalX, finalY }
transform = ''
```

清掉 transform 后，新的 `left/top` 就是松手位置。结果是“提交拖动位置”，不是“回到卡片位”。

应该保留 `cardPosition` 为动态中心，transition 只把 `transform` 从当前 `dx/dy` 动画回 `0,0`。

### 16.2 `indicatorProgress` 没有在移动时赋值

位置：

- 声明：`js/framework/use-card-mode.js:103`
- 消费：`js/framework/use-card-mode.js:233-235`
- 移动计算：`js/framework/use-card-mode.js:421-452`

`updateIndicatorGesture()` 计算了 `linear` 和 `eased`，但没有执行 `indicatorProgress.value = eased`。因此 `syncDesktopEffect()` 读取到的进度仍是 0，Home Indicator 拖动期间的桌面 blur 不会随手势增长。

### 16.3 切入 card 后没有再次同步桌面效果

`convertToCard()` 在 `appViewMode` 仍为 full 时先把 `indicatorGesture.active` 设为 false 并调用 `syncDesktopEffect()`，这会把 blur 归零；随后才设置 `appViewMode = 'card'`，但之后没有再次调用 `syncDesktopEffect()`。

所以即使 card 分支规定 blur 为最大值，这次模式切换也没有明确触发该同步函数。

### 16.4 当前在移动中越过阈值就提交，参考系统在松手时提交

位置：`js/framework/use-card-mode.js:421-453`。

当前 `updateIndicatorGesture()` 在 `absMove >= CONVERT_TO_CARD_THRESHOLD` 时立即调用 `convertToCard()`。参考系统则把 `abs(moveY) > 120` 的判断放在 `handleEnd()` 中。

这意味着：

- 参考系统在手指抬起前始终可取消；用户可以先拖过阈值，再往回拖到阈值内后松手，结果仍是全屏回弹。
- 当前系统第一次越过 100px 就切换到 card，即使手指仍按着，也不能把这次手势收回。
- 当前 `convertToCard()` 会在 pointer 序列尚未结束时清理 indicator 状态并切换模板 class，增大了入场动画、Pointer Capture 和后续 click 抑制互相干扰的概率。

若目标是复刻参考手感，应只在 move 阶段更新视觉进度，把“进入 card 或回 full”的提交判定放到 pointerup/mouseup。

### 16.5 JS 最后一帧与 CSS keyframe 起点方向相反

位置：

- JS：`js/framework/use-card-mode.js:190-205`
- CSS：`css/main.css:1344-1374`

JS 的 Home Indicator 手势末态使用负向上移，目标约为：

```text
translateY(-38px) scale(0.718)
```

但 `cardJustEntered` 关键帧 0% 写的是：

```text
translateY(52px) scale(0.718)
```

一个在上，一个在下。切换 class 时 animation 会接管 transform，容易从手指最后一帧跳到另一侧的固定起点。

CSS 注释也仍描述旧的 `+52px` 起点，与当前 JS 的 `-38px` 不一致。

需要说明的是：当前 `index.html:242-257` 没有把 `cardJustEntered` 暴露的 `cardJustEnteredAt` 绑到模板，所以这套 CSS 关键帧当前并未真正进入播放链。问题 16.5 描述的是“一旦接通就会反向起跳”的潜在风险，而不是“当前用户能看到的反向起跳”。

### 16.6 `convertToCard()` 的注释与实际 style 所有权矛盾

`use-card-mode.js:347-357` 的注释说 Vue 会用 card 分支的新 transform 覆盖 inline transform；但 `activeAppShellStyle()` 在 `use-card-mode.js:131-160` 又明确保证永远不返回 transform 和 borderRadius。

实际 transform 主要由 DOM 直写和 CSS animation 管理。注释反映了实现经历过多轮修改，当前所有权边界已经不一致。

### 16.7 `pointercancel` 不会把卡片视觉复位

`cancelCardDrag()` 只清：

- `cardDrag.active`
- `cardDrag.moved`
- `isDraggingCard`

它没有恢复：

- `shell.style.transform`
- `shell.style.opacity`
- `shell.style.transition`

系统级手势打断、浏览器取消指针或设备切换时，卡片可能停留在半拖动姿态。

### 16.8 入场期间仍可起第二段卡片拖拽

参考系统等待 400ms 后才绑定卡片拖拽。当前模板始终绑定 card pointer 事件，只靠 `appViewMode === 'card'` 判断。`convertToCard()` 一设置 card，用户就可以在 `cardJustEntered` 动画尚未结束时起手，DOM 直写与 CSS animation 可能竞争 transform。

应增加明确的 `isCardEntering` 状态，入场结束前不接受卡片拖拽。

### 16.9 `suppressNextCardClick` 可能吞掉真正的第一次点击

进入 card 时它被设为 true，只有后续 click 才清除，没有定时过期。若上滑手势本身没有生成 click，用户动画结束后的第一次点击会被当成“应抑制的旧 click”。

参考系统的 50ms 拖拽标志窗口更符合事件时序。

### 16.10 若干状态或函数已经没有有效用途

从当前静态引用看：

- `resetShellToCardPosition()` 定义后没有被调用。
- `desktopElement` 被暴露，但拖拽逻辑没有使用它。
- `cardScale` 与 `cardBorderRadius` 会被赋值，但 `activeAppShellStyle()` 不读取它们。
- move rAF 内的 `distance` 变量没有被使用。

这些遗留项会增加维护者误判“谁负责 transform / radius / 回弹”的风险。

### 16.11 圆角常量存在 25px 与 28px 两套值

- `UI_CONSTANTS.CARD_RADIUS = 25`
- `.app-shell.card { border-radius: 28px }`

同时 `cardJustEntered` 的终点也是 28px。当前 DOM 直写、响应式状态和 CSS 最终值并不统一。

### 16.12 rAF 回调没有使用这一帧最新的 `dx/dy`

位置：`js/framework/use-card-mode.js:619-663`。

每次 pointermove 都会更新：

```text
pendingMove = { x: nextX, y: nextY }
```

但第一次安排 `requestAnimationFrame()` 时，回调闭包捕获的是那一次事件的 `dx/dy`。同一帧内后续 pointermove 虽然替换了 `pendingMove`，DOM 直写仍使用旧闭包里的：

```text
transform(dx, dy)
opacity(dx, dy)
rotate(dx)
```

`pendingMove` 中最新的 `x/y` 只在响应式 fallback 路径使用。因此当前实现做到了“一帧只写一次”，却没有完全做到“每帧使用最后一次指针位置”。快速拖动时卡片可能比手指慢一个事件采样。

应把最新的 `dx/dy` 一起放进 `pendingMove`，rAF 回调只读取 `move.dx/move.dy`。

### 16.13 动态中心被反复测量，但没有同步到卡片位置

位置：`js/framework/use-card-mode.js:131-161`、`647-648`。

card 模式 computed 和拖动 rAF 都会执行 `dynamicCardCenter = computeCardCenter()`，但没有把新中心写入 `cardPosition`。`activeAppShellStyle` 最终返回的仍是旧的 `cardPosition.value.x/y`。

所以当前代码具备适配手机壳/fullscreen 的测量函数，但卡片已经显示后若容器尺寸或手机壳模式变化，单纯重新测量不会自动重居中。应在明确的 resize/fullscreen 变化或回弹提交点同步中心，而不是在每次拖动帧中做一次未使用的布局测量。

### 16.14 卡片态没有完全冻结内部业务 action

参考系统进入卡片后直接设置 `appContent.style.pointerEvents = 'none'`。当前 CSS `css/main.css:1338-1342` 只把 `.app-page/.app-detail-page` 改成 `overflow: hidden; touch-action: none`，解决滚动抢手势，但 `index.html:269` 的 `.app-content @click="handleAppContentClick"` 仍然存在。

因此，尤其在鼠标点击路径中，卡片内部带 `data-app-action` 的元素仍可能先执行 `handleAppContentClick()`，随后外层 shell 再处理卡片点击。若要复刻参考行为，应在 card 模式让业务内容层不接收 pointer/click，同时保持 `.app-shell` 外层可以拖动与点击恢复。

---

## 17. 推荐的现代化复刻方案

目标是复刻参考行为，不复刻参考架构。

### 17.1 用一个明确阶段状态替代互相重叠的布尔量

建议至少区分：

```text
full
indicator-dragging
card-entering
card-idle
card-dragging
card-dismissing
```

`appViewMode` 仍可保留为 `full/card` 给模板控制布局，但手势内部要知道 card 是正在进入、静止、拖动还是退出。

### 17.2 把 DOM 属性所有权固定下来

建议：

- Vue/CSS 负责：`left/top/width/height` 的 full/card 基础布局、基础圆角和阴影。
- 拖拽控制器负责：热路径 `transform/opacity`。
- 模式转换控制器负责：transition 的临时关闭与恢复。
- 不再让 CSS keyframe、Vue computed 和 DOM 直写同时争夺 transform。

### 17.3 Home Indicator 拖动使用参考公式或等价进度

若追求最像参考，可直接使用：

```text
moveY = min(0, currentY - startY)
scale = max(0.6, 1 - abs(moveY) / 800)
translateY = moveY × 0.5
blur = min(20, abs(moveY) / 30)
radius = min(50, 40 + abs(moveY) / 15)
```

如果保留当前进度方案，也必须：

- 每帧同步 `indicatorProgress`。
- 保证 JS 手势末态就是进入卡片动画的真实起点。
- 不再播放方向相反的固定起点 keyframe。

### 17.4 进入卡片使用“最后一帧 → 目标布局”的 transition

推荐流程：

1. 拖动期 `transition: none`，DOM 保留最后一帧 transform。
2. 记录最后一帧姿态，不清 transform。
3. 设置 `appViewMode = card`，让基础布局切到居中 280×400。
4. 下一帧恢复 transition。
5. 把 transform 动画到 `translate3d(0,0,0) scale(1) rotate(0)`。
6. 过渡结束后清理 inline transition/transform。
7. 进入 `card-idle` 后才允许新拖拽。

不需要 `cardJustEntered` 固定关键帧。若要 overshoot，应使用从实际手势姿态生成的 Web Animations/CSS 变量，不能写死相反方向的起点。

### 17.5 卡片拖动继续使用当前高性能方式

保留：

- `dx/dy`
- 8px 或调整为参考的 5px 启动阈值
- rAF 合帧
- `translate3d(dx,dy,0)`
- `opacity = max(0.3, 1-distance/400)`
- `rotate = clamp(dx/20, -18, 18)`

不要退回逐帧 `left/top`。

### 17.6 松手不足 100px 时只回 transform，不提交位置

基础 `left/top` 始终保持最新卡片中心。

松手时：

```text
transition = transform/opacity 回弹曲线
transform → translate3d(0,0,0) scale(1) rotate(0)
opacity → 1
```

不要把 `origin + dx/dy` 写回 `cardPosition`。

### 17.7 关闭动画按需求选择“严格参考”

如果目标是严格像参考系统：

```text
translate3d(0, -200px, 0) scale(0.5) rotate(0deg)
opacity: 0
```

如果保留当前方向飞出，则文档和设计应明确这是有意差异，而不是参考复刻。

### 17.8 卡片态冻结业务交互

当前 CSS 已冻结 `.app-page/.app-detail-page` 的滚动，但还应保证：

- 卡片态内容按钮不执行 App action。
- 卡片态 Tab/Nav/Home Indicator 不响应。
- 点击卡片只负责恢复全屏。
- 拖动后产生的 click 被短时间抑制。

可以用 card class 下统一 `pointer-events: none` 给内容层，再让外层 shell 保持 pointer events。

### 17.9 取消手势必须等价于“不足阈值松手”

`pointercancel`、窗口失焦、active App 变化时都应：

- 取消 rAF。
- 释放 pointer capture（若存在）。
- 回到中心 transform。
- opacity 回到 1。
- 恢复 transition 所有权。
- 清除拖拽与点击抑制状态。

---

## 18. 建议改造顺序

### 第一批：修正确认的行为错误

1. 修复不足 100px 松手后不回中心。
2. 在 Home Indicator move 时写入 `indicatorProgress`。
3. 把进入 card 的阈值提交从 `pointermove` 移到 `pointerup/mouseup`，恢复“松手前可取消”。
4. 切换 card 后同步桌面 blur。
5. 让 rAF 回调使用 `pendingMove` 中最新的 `dx/dy`。
6. 修复 `pointercancel` 视觉残留。
7. 给 `suppressNextCardClick` 加短时有效窗口，或改回基于真实拖拽的 click 抑制。
8. 卡片态冻结业务内容的 pointer/click，只保留外层卡片手势。

### 第二批：统一进入卡片的动画所有权

1. 删除或停用写死起点的 `cardJustEntered` transform keyframe。
2. 让最后一帧手势姿态直接 transition 到 card 基础布局。
3. 增加 `card-entering` 阶段，动画结束前禁止第二段拖拽。
4. 统一 25px/28px 圆角。

### 第三批：收敛代码

1. 删除未使用的 `resetShellToCardPosition()`、`desktopElement` 或真正接入。
2. 删除无效的 `cardScale/cardBorderRadius`，或重新定义其职责。
3. 把 transition 时长和曲线集中为常量。
4. 只在 resize/fullscreen 变化与回弹提交点同步动态中心，删除拖动热路径中未使用的测量。
5. 清理与现状相反的旧注释。

### 第四批：可选增强，不应与复刻混做

1. 增加速度/投影距离判定。
2. 根据拖动方向加入轻微非线性阻尼。
3. 支持 `prefers-reduced-motion`。
4. 在真实移动设备上按 60Hz/120Hz 做帧率与输入延迟测试。

---

## 19. 复刻完成后的验收标准

### 19.1 Home Indicator

- 手指按下后窗口立即跟手，没有 transition 追赶感。
- 向下移动不改变窗口。
- 上滑不足阈值松手，窗口连续回到全屏。
- 上滑超过阈值，窗口从松手前最后一帧连续变成卡片，不能先跳到另一处。
- 拖动期间桌面 blur 随距离变化，进入卡片后保持稳定。

### 19.2 卡片入场

- 卡片最终尺寸不变。
- 水平居中，垂直中心上移 30px。
- 入场动画期间不能被第二个拖拽打断。
- 入场结束后的第一次真实点击能够恢复全屏，不能被永久 click 抑制标志吞掉。

### 19.3 卡片拖动

- 5～8px 内仍视为点击，不启动拖拽。
- 越过阈值后卡片使用 transform 跟手。
- 透明度按二维距离下降，最低 0.3。
- 横向拖动带旋转，最大角度受限。
- 内部滚动区、按钮、Tab 不抢拖拽。

### 19.4 卡片松手

- 二维距离不超过 100px：无论拖到哪个方向，都回到最新计算的中心。
- 回弹后 transform 为 0、opacity 为 1，响应式位置仍是中心。
- 二维距离超过 100px：播放统一关闭动画并调用 `closeApp()`。
- 拖后 click 不得误触发恢复全屏或业务按钮。

### 19.5 异常路径

- `pointercancel` 后不能卡在半透明、旋转或 transition none 状态。
- App 在拖动期间被外部关闭时，rAF 与所有临时状态被清掉。
- 手机壳显示/隐藏与 fullscreen 模式下都能重新计算中心。
- 连续进入、恢复、再进入卡片，不继承上一次位移。

---

## 20. 最终判断

参考系统值得保留的是**行为模型**：

- 同一个外层窗口贯穿全屏、卡片、恢复和关闭。
- 拖动期 transition 关闭，窗口直接跟手。
- 松手结果是确定的二选一。
- 卡片不足关闭阈值必定回原中心。
- 从手势最后一帧连续落位，不从另一套固定动画起点重新开始。
- 卡片态冻结内部交互。
- 关闭统一向上飞出。

参考系统不值得保留的是**实现年代**：

- 全局变量与原型继承。
- touch/mouse 双份代码。
- 每帧修改 left/top。
- `transition: all`。
- 无 rAF、无 Pointer Capture、无 cancel 收口。

当前 `use-card-mode.js` 已经有更好的技术底座，但现在同时混用了 Vue style、DOM 直写和 CSS keyframe，且松手回弹、桌面 blur、入场起点和 click 抑制存在已确认的不一致。只要先收拢属性所有权，再按本文状态机修正结果，它可以比参考系统更流畅，同时在交互表现上更接近参考系统。

---

## 附录 A：`参考/core.js` 卡片拖拽完整代码

以下是从 `参考/core.js` 中提取的所有与卡片拖拽相关的代码，按功能模块组织。行号为源文件中的实际位置。

### A.1 BaseApp 基类定义（2582-2645）

```2582:2645:参考/core.js
    // ============ 12. 统一App基类（合并MyBaseApp和EnhancedApp）============
    /* 【统一App基类】将原来分散在index.html中的MyBaseApp和core.js中的EnhancedApp合并
       优点：
       1. 减少继承层级，代码更清晰
       2. 所有App相关功能集中管理
       3. 避免重复定义相同的方法

       使用方式：
       var myApp = new BaseApp({
           id: 'my-app',
           name: '我的应用',
           color: '#fff',
           statusBarStyle: 'dark',
           tabs: [...],
           island: {...}
       }); */
    function BaseApp(config) {
        /* 【基础属性】 */
        this.id = config.id;
        this.name = config.name;
        this.icon = config.icon || '';
        this.color = config.color;
        this.barStyle = config.barStyle || 'dark';
        this.statusBarStyle = config.statusBarStyle || config.barStyle || 'dark';
        this.tabs = config.tabs || [];
        this.island = config.island || null;

        /* 【窗口状态】 */
        this.iconEl = document.getElementById(this.id);
        this.appWindow = null;
        this.currentTabIndex = 0;
        this.isCardMode = false;
        this.isCardDragging = false;
        this.pageStack = [];
        this.detailPageHomeIndicatorEnabled = config.detailPageHomeIndicatorEnabled !== false;

        /* 【增强功能】角标、小组件、分享等 */
        this.badge = 0;
        this.widgets = config.widgets || [];
        this.shareTypes = config.shareTypes || [];
        this.npcPrompt = config.npcPrompt || '';

        /* 【AI可见信息配置】
           扩展App可以声明自己可以提供给AI的数据，会自动注册到系统配置中
           格式：{
               enabled: true,                    // 是否启用（默认true）
               name: '应用名称',                 // 在配置界面显示的名称
               icon: '<svg...>',                 // 图标（SVG字符串）
               desc: 'AI可读取的数据描述',       // 描述
               dataSources: [                    // 可配置的数据源列表
                   { id: 'source1', name: '数据源1', desc: '数据源描述', default: true },
                   ...
               ]
           } */
        this.aiVisibility = config.aiVisibility || null;

        /* 【缓存机制】避免重复创建DOM */
        this.windowCache = null;
        this.contentCache = {};

        /* 【绑定图标点击事件】
           使用立即绑定和延迟绑定两种方式确保可靠性 */
        this.bindIconClick();
    }

---

### A.2 渲染 App 窗口（2784-2857）

```javascript
    /* 【渲染App窗口】使用缓存机制，避免重复创建DOM */
    BaseApp.prototype.render = function() {
        /* 【缓存检查】如果已经渲染过，直接返回 */
        if (this.windowCache && this.appWindow) {
            return;
        }

        var self = this;
        var win = document.createElement('div');
        win.className = 'app-window hidden';
        // 支持渐变背景
        if (this.color && this.color.indexOf('gradient') !== -1) {
            win.style.background = this.color;
        } else {
            win.style.backgroundColor = this.color;
        }

        var indicatorColor = this.barStyle === 'light' ? 'rgba(255,255,255,0.6)' : 'rgba(0,0,0,0.3)';

        // 从渐变中提取首尾颜色，用于顶部和底部区域
        var topBgStyle = '';
        var bottomBgStyle = '';
        if (this.color && this.color.indexOf('gradient') !== -1) {
            // 提取渐变中的颜色值
            var colorMatches = this.color.match(/#[0-9A-Fa-f]{3,8}|rgba?\([^)]+\)|[a-zA-Z]+(?=\s+\d)/g);
            if (colorMatches && colorMatches.length >= 1) {
                topBgStyle = 'background-color:' + colorMatches[0] + ';';
                bottomBgStyle = 'background-color:' + colorMatches[colorMatches.length - 1] + ';';
            }
        } else if (this.color) {
            topBgStyle = 'background-color:' + this.color + ';';
            bottomBgStyle = 'background-color:' + this.color + ';';
        }

        var tabBarHtml = '';
        var tabContentHtml = '';

        if (this.tabs.length > 0) {
            var tabItems = this.tabs.map(function(tab, index) {
                var activeClass = index === 0 ? 'active' : '';
                return '<div class="app-tab-item ' + activeClass + '" data-index="' + index + '">' +
                           '<span class="tab-icon">' + tab.icon + '</span>' +
                           '<span>' + tab.name + '</span>' +
                       '</div>';
            }).join('');

            tabBarHtml = '<div class="app-tab-bar">' + tabItems + '</div>';
            tabContentHtml = this.tabs[0].content || '';
        }

        win.innerHTML =
            '<div class="app-status-bar-gap" style="' + topBgStyle + '"></div>' +
            '<div class="app-page-stack">' +
                '<div class="app-content-page" id="main-content-area">' + tabContentHtml + '</div>' +
            '</div>' +
            tabBarHtml +
            '<div class="app-bottom-indicator-wrap" style="' + bottomBgStyle + '">' +
                '<div class="home-indicator-area"></div>' +
                '<div class="home-indicator" style="background-color:' + indicatorColor + ';"></div>' +
            '</div>';

        document.getElementById('appContainer').appendChild(win);
        this.appWindow = win;

        this.bindTabEvents();
        this.bindHomeIndicatorEvents();

        if (this.island && this.island.onAppReady) {
            this.island.onAppReady(this.appWindow, this);
        }

        /* 【标记已缓存】 */
        this.windowCache = true;
    };
```

### A.3 打开详情页（2885-2956）

```javascript
    /* 【打开详情页】支持页面堆栈 */
    BaseApp.prototype.openDetailPage = function(contentHtml, options) {
        if (!this.appWindow) return;

        options = options || {};
        var self = this;
        var indicatorColor = this.barStyle === 'light' ? 'rgba(255,255,255,0.6)' : 'rgba(0,0,0,0.3)';
        var backBtnColor = this.barStyle === 'light' ? '#fff' : '#007AFF';
        var enableHomeIndicator = options.enableHomeIndicator !== false && this.detailPageHomeIndicatorEnabled;

        // 详情页背景
        var detailBg = options.background || this.color;

        // 从渐变中提取首尾颜色
        var topBgStyle = '';
        var bottomBgStyle = '';
        if (detailBg && detailBg.indexOf('gradient') !== -1) {
            var colorMatches = detailBg.match(/#[0-9A-Fa-f]{3,8}|rgba?\([^)]+\)|[a-zA-Z]+(?=\s+\d)/g);
            if (colorMatches && colorMatches.length >= 1) {
                topBgStyle = 'background-color:' + colorMatches[0] + ';';
                bottomBgStyle = 'background-color:' + colorMatches[colorMatches.length - 1] + ';';
            }
        } else if (detailBg) {
            topBgStyle = 'background-color:' + detailBg + ';';
            bottomBgStyle = 'background-color:' + detailBg + ';';
        }

        var page = document.createElement('div');
        page.className = 'app-page slide-in';
        page.style.cssText = 'position:absolute; top:0; left:0; width:100%; height:100%; ' +
                             'background:' + detailBg + '; z-index:100; display:flex; flex-direction:column;';

        page.innerHTML =
            '<div class="app-status-bar-gap" style="' + topBgStyle + '"></div>' +
            '<div class="app-back-btn" style="color:' + backBtnColor + ';">返回</div>' +
            '<div class="app-content-page" style="flex:1; overflow-y:auto; padding:0;">' +
                contentHtml +
            '</div>' +
            '<div class="app-bottom-indicator-wrap" style="' + bottomBgStyle + '">' +
                '<div class="home-indicator" style="background:' + indicatorColor + ';"></div>' +
                (enableHomeIndicator ? '<div class="home-indicator-area detail-home-indicator"></div>' : '') +
            '</div>';

        var backBtn = page.querySelector('.app-back-btn');
        backBtn.onclick = function() {
            // 先调用返回回调，如果返回false则取消关闭
            if (options.onBack) {
                var result = options.onBack();
                if (result === false) {
                    return; // 取消关闭
                }
            }
            page.classList.remove('slide-in');
            page.classList.add('slide-out');
            setTimeout(function() {
                page.remove();
                self.pageStack.pop();
            }, 300);
        };

        if (enableHomeIndicator) {
            var homeIndicatorArea = page.querySelector('.detail-home-indicator');
            if (homeIndicatorArea) {
                this.bindDetailPageHomeIndicator(homeIndicatorArea, page);
            }
        }

        this.appWindow.appendChild(page);
        this.pageStack.push(page);

        return page;
    };
```

### A.4 详情页 Home 指示器事件绑定（3065-3134）

```javascript
    /* 【详情页Home指示器事件绑定】 */
    BaseApp.prototype.bindDetailPageHomeIndicator = function(handle, page) {
        var self = this;
        var startY = 0;
        var moveY = 0;
        var isDragging = false;

        function handleStart(clientY) {
            if (self.isCardMode) return;
            startY = clientY;
            moveY = 0;
            isDragging = true;
            self.appWindow.style.transition = 'none';
        }

        function handleMove(clientY) {
            if (!isDragging || self.isCardMode) return;
            moveY = clientY - startY;

            if (moveY > 0) { moveY = 0; return; }

            var scale = 1 - Math.abs(moveY) / 800;
            if (scale < 0.6) scale = 0.6;

            var blur = Math.abs(moveY) / 30;
            if (blur > 20) blur = 20;

            var borderRadius = 40 + Math.abs(moveY) / 15;
            if (borderRadius > 50) borderRadius = 50;

            self.appWindow.style.transform = 'translateY(' + (moveY * 0.5) + 'px) scale(' + scale + ')';
            self.appWindow.style.borderRadius = borderRadius + 'px';
            document.getElementById('desktop').style.filter = 'blur(' + blur + 'px)';
        }

        function handleEnd() {
            if (!isDragging || self.isCardMode) return;
            isDragging = false;

            self.appWindow.style.transition = 'all 0.4s cubic-bezier(0.2,0.8,0.2,1)';

            if (Math.abs(moveY) > 120) {
                self.switchToCardMode();
            } else {
                self.appWindow.style.transform = '';
                self.appWindow.style.borderRadius = '40px';
                document.getElementById('desktop').style.filter = 'blur(0px)';
            }
        }

        handle.ontouchstart = function(e) { e.preventDefault(); handleStart(e.touches[0].clientY); };
        handle.ontouchmove = function(e) { e.preventDefault(); handleMove(e.touches[0].clientY); };
        handle.ontouchend = function(e) { e.preventDefault(); handleEnd(); };

        handle.onmousedown = function(e) {
            e.preventDefault();
            handleStart(e.clientY);

            function mouseMoveHandler(e) { e.preventDefault(); handleMove(e.clientY); }
            function mouseUpHandler(e) {
                e.preventDefault();
                handleEnd();
                document.removeEventListener('mousemove', mouseMoveHandler);
                document.removeEventListener('mouseup', mouseUpHandler);
            }

            document.addEventListener('mousemove', mouseMoveHandler);
            document.addEventListener('mouseup', mouseUpHandler);
        };
    };
```

### A.5 打开 App（3136-3194）

```javascript
    /* 【打开App】带缓存和动画 */
    BaseApp.prototype.open = function() {
        if (!this.appWindow) {
            this.render();
        }

        var self = this;

        /* 【更新状态栏样式】 */
        var statusBar = document.querySelector('.status-bar');
        if (statusBar) {
            statusBar.classList.remove('status-bar-light', 'status-bar-dark', 'status-bar-transparent');
            statusBar.classList.add('status-bar-' + this.statusBarStyle);
        }

        /* 【安全获取图标位置】确保iconEl存在 */
        if (!this.iconEl) {
            this.iconEl = document.getElementById(this.id);
        }

        var centerX = 200, centerY = 400;  // 默认从屏幕中央展开

        if (this.iconEl) {
            var iconRect = this.iconEl.getBoundingClientRect();
            var screenRect = document.getElementById('phone-screen').getBoundingClientRect();
            var startX = iconRect.left - screenRect.left;
            var startY = iconRect.top - screenRect.top;
            centerX = startX + iconRect.width / 2;
            centerY = startY + iconRect.height / 2;
        }

        this.appWindow.style.left = '0';
        this.appWindow.style.top = '0';
        this.appWindow.style.width = '100%';
        this.appWindow.style.height = '100%';
        this.appWindow.style.transformOrigin = centerX + 'px ' + centerY + 'px';
        this.appWindow.style.transform = 'scale(0.1)';
        this.appWindow.style.opacity = '0';
        this.appWindow.style.borderRadius = '40px';
        this.appWindow.style.transition = 'none';

        this.appWindow.classList.remove('hidden');
        this.appWindow.offsetHeight;

        this.appWindow.style.transition = 'transform 0.4s cubic-bezier(0.2,0.8,0.2,1), ' +
                                           'opacity 0.4s cubic-bezier(0.2,0.8,0.2,1), ' +
                                           'border-radius 0.4s cubic-bezier(0.2,0.8,0.2,1)';

        requestAnimationFrame(function() {
            self.appWindow.style.transform = 'scale(1)';
            self.appWindow.style.opacity = '1';
            self.appWindow.style.borderRadius = '40px';
        });

        setTimeout(function() {
            self.appWindow.style.transition = '';
            self.appWindow.style.transformOrigin = '';
        }, 400);
    };
```

### A.6 主页面 Home 指示器事件绑定（3196-3268）

```javascript
    /* 【Home指示器事件绑定】 */
    BaseApp.prototype.bindHomeIndicatorEvents = function() {
        var self = this;
        var handle = this.appWindow.querySelector('.home-indicator-area');
        if (!handle) return;

        var startY = 0;
        var moveY = 0;
        var isDragging = false;

        function handleStart(clientY) {
            if (self.isCardMode) return;
            startY = clientY;
            moveY = 0;
            isDragging = true;
            self.appWindow.style.transition = 'none';
        }

        function handleMove(clientY) {
            if (!isDragging || self.isCardMode) return;
            moveY = clientY - startY;

            if (moveY > 0) { moveY = 0; return; }

            var scale = 1 - Math.abs(moveY) / 800;
            if (scale < 0.6) scale = 0.6;

            var blur = Math.abs(moveY) / 30;
            if (blur > 20) blur = 20;

            var borderRadius = 40 + Math.abs(moveY) / 15;
            if (borderRadius > 50) borderRadius = 50;

            self.appWindow.style.transform = 'translateY(' + (moveY * 0.5) + 'px) scale(' + scale + ')';
            self.appWindow.style.borderRadius = borderRadius + 'px';
            document.getElementById('desktop').style.filter = 'blur(' + blur + 'px)';
        }

        function handleEnd() {
            if (!isDragging || self.isCardMode) return;
            isDragging = false;

            self.appWindow.style.transition = 'all 0.4s cubic-bezier(0.2,0.8,0.2,1)';

            if (Math.abs(moveY) > 120) {
                self.switchToCardMode();
            } else {
                self.appWindow.style.transform = '';
                self.appWindow.style.borderRadius = '40px';
                document.getElementById('desktop').style.filter = 'blur(0px)';
            }
        }

        handle.ontouchstart = function(e) { e.preventDefault(); handleStart(e.touches[0].clientY); };
        handle.ontouchmove = function(e) { e.preventDefault(); handleMove(e.touches[0].clientY); };
        handle.ontouchend = function(e) { e.preventDefault(); handleEnd(); };

        handle.onmousedown = function(e) {
            e.preventDefault();
            handleStart(e.clientY);

            function mouseMoveHandler(e) { e.preventDefault(); handleMove(e.clientY); }
            function mouseUpHandler(e) {
                e.preventDefault();
                handleEnd();
                document.removeEventListener('mousemove', mouseMoveHandler);
                document.removeEventListener('mouseup', mouseUpHandler);
            }

            document.addEventListener('mousemove', mouseMoveHandler);
            document.addEventListener('mouseup', mouseUpHandler);
        };
    };
```

### A.7 切换到卡片模式（3270-3316）

```javascript
    /* 【切换到卡片模式】 */
    BaseApp.prototype.switchToCardMode = function() {
        var self = this;
        this.isCardMode = true;

        var screen = document.getElementById('phone-screen');
        var screenRect = screen.getBoundingClientRect();

        var cardWidth = 280;
        var cardHeight = 400;
        var targetX = (screenRect.width - cardWidth) / 2;
        var targetY = (screenRect.height - cardHeight) / 2 - 30;

        this.appWindow.style.transition = 'all 0.4s cubic-bezier(0.2,0.8,0.2,1)';
        this.appWindow.style.position = 'absolute';
        this.appWindow.style.left = targetX + 'px';
        this.appWindow.style.top = targetY + 'px';
        this.appWindow.style.width = cardWidth + 'px';
        this.appWindow.style.height = cardHeight + 'px';
        this.appWindow.style.transform = 'scale(1)';
        this.appWindow.style.borderRadius = '25px';
        this.appWindow.style.boxShadow = '0 20px 50px rgba(0,0,0,0.3)';

        document.getElementById('desktop').style.filter = 'blur(20px)';

        var appContent = this.appWindow.querySelector('.app-content-page');
        var homeIndicator = this.appWindow.querySelector('.home-indicator');
        var tabBar = this.appWindow.querySelector('.app-tab-bar');
        var homeIndicatorArea = this.appWindow.querySelector('.home-indicator-area');

        if (appContent) appContent.style.pointerEvents = 'none';
        if (homeIndicator) homeIndicator.style.display = 'none';
        if (tabBar) tabBar.style.display = 'none';
        if (homeIndicatorArea) homeIndicatorArea.style.display = 'none';

        setTimeout(function() {
            self.appWindow.style.transition = '';
            self.bindCardDragEvents();

            self.appWindow.onclick = function(e) {
                if (self.isCardMode && !self.isCardDragging) {
                    e.stopPropagation();
                    self.restoreFromCardMode();
                }
            };
        }, 400);
    };
```

### A.8 从卡片模式恢复（3318-3351）

```javascript
    /* 【从卡片模式恢复】 */
    BaseApp.prototype.restoreFromCardMode = function() {
        var self = this;
        this.isCardMode = false;

        this.appWindow.style.transition = 'all 0.4s cubic-bezier(0.2,0.8,0.2,1)';
        this.appWindow.style.position = 'absolute';
        this.appWindow.style.left = '0';
        this.appWindow.style.top = '0';
        this.appWindow.style.width = '100%';
        this.appWindow.style.height = '100%';
        this.appWindow.style.transform = 'scale(1)';
        this.appWindow.style.borderRadius = '40px';
        this.appWindow.style.boxShadow = 'none';

        document.getElementById('desktop').style.filter = 'blur(0px)';

        var appContent = this.appWindow.querySelector('.app-content-page');
        var homeIndicator = this.appWindow.querySelector('.home-indicator');
        var tabBar = this.appWindow.querySelector('.app-tab-bar');
        var homeIndicatorArea = this.appWindow.querySelector('.home-indicator-area');

        if (appContent) appContent.style.pointerEvents = 'auto';
        if (homeIndicator) homeIndicator.style.display = '';
        if (tabBar) tabBar.style.display = '';
        if (homeIndicatorArea) homeIndicatorArea.style.display = '';

        this.appWindow.onclick = null;

        setTimeout(function() {
            self.appWindow.style.transition = '';
            self.appWindow.style.position = '';
        }, 400);
    };
```

### A.9 卡片模式拖拽事件绑定（3353-3452）

```javascript
    /* 【卡片模式拖拽事件绑定】 */
    BaseApp.prototype.bindCardDragEvents = function() {
        var self = this;

        var cardStartX = 0;
        var cardStartY = 0;
        var cardMoveX = 0;
        var cardMoveY = 0;
        var cardStartLeft = 0;
        var cardStartTop = 0;

        function handleCardStart(clientX, clientY) {
            if (!self.isCardMode) return;

            cardStartX = clientX;
            cardStartY = clientY;
            cardMoveX = 0;
            cardMoveY = 0;
            self.isCardDragging = false;

            cardStartLeft = parseFloat(self.appWindow.style.left) || 0;
            cardStartTop = parseFloat(self.appWindow.style.top) || 0;

            self.appWindow.style.transition = 'none';
        }

        function handleCardMove(clientX, clientY) {
            if (!self.isCardMode) return;

            cardMoveX = clientX - cardStartX;
            cardMoveY = clientY - cardStartY;

            if (Math.abs(cardMoveX) > 5 || Math.abs(cardMoveY) > 5) {
                self.isCardDragging = true;
            }

            if (self.isCardDragging) {
                self.appWindow.style.left = (cardStartLeft + cardMoveX) + 'px';
                self.appWindow.style.top = (cardStartTop + cardMoveY) + 'px';

                var distance = Math.sqrt(cardMoveX * cardMoveX + cardMoveY * cardMoveY);
                var opacity = 1 - distance / 400;
                var rotate = cardMoveX / 20;

                self.appWindow.style.opacity = Math.max(0.3, opacity);
                self.appWindow.style.transform = 'rotate(' + rotate + 'deg)';
            }
        }

        function handleCardEnd() {
            if (!self.isCardMode) return;

            if (!self.isCardDragging) {
                self.appWindow.style.transition = '';
                return;
            }

            self.appWindow.style.transition = 'all 0.3s ease-out';
            var distance = Math.sqrt(cardMoveX * cardMoveX + cardMoveY * cardMoveY);

            if (distance > 100) {
                self.exit();
            } else {
                self.appWindow.style.left = cardStartLeft + 'px';
                self.appWindow.style.top = cardStartTop + 'px';
                self.appWindow.style.opacity = '1';
                self.appWindow.style.transform = 'rotate(0deg)';
            }

            setTimeout(function() { self.isCardDragging = false; }, 50);
        }

        this.appWindow.ontouchstart = function(e) {
            if (!self.isCardMode) return;
            handleCardStart(e.touches[0].clientX, e.touches[0].clientY);
        };
        this.appWindow.ontouchmove = function(e) {
            if (!self.isCardMode) return;
            handleCardMove(e.touches[0].clientX, e.touches[0].clientY);
        };
        this.appWindow.ontouchend = function(e) {
            if (!self.isCardMode) return;
            handleCardEnd();
        };

        this.appWindow.onmousedown = function(e) {
            if (!self.isCardMode) return;
            handleCardStart(e.clientX, e.clientY);

            function mouseMoveHandler(e) { handleCardMove(e.clientX, e.clientY); }
            function mouseUpHandler(e) {
                handleCardEnd();
                document.removeEventListener('mousemove', mouseMoveHandler);
                document.removeEventListener('mouseup', mouseUpHandler);
            }

            document.addEventListener('mousemove', mouseMoveHandler);
            document.addEventListener('mouseup', mouseUpHandler);
        };
    };
```

### A.10 退出 App（3454-3515）

```javascript
    /* 【退出App】保留DOM，只隐藏 */
    BaseApp.prototype.exit = function() {
        var self = this;

        this.appWindow.style.transition = 'all 0.3s ease-out';

        if (this.isCardMode) {
            this.appWindow.style.transform = 'translateY(-200px) scale(0.5) rotate(0deg)';
            this.appWindow.style.opacity = '0';
        } else {
            var iconRect = this.iconEl.getBoundingClientRect();
            var screenRect = document.getElementById('phone-screen').getBoundingClientRect();
            var centerX = iconRect.left - screenRect.left + iconRect.width / 2;
            var centerY = iconRect.top - screenRect.top + iconRect.height / 2;

            this.appWindow.style.transformOrigin = centerX + 'px ' + centerY + 'px';
            this.appWindow.style.transform = 'scale(0.1)';
            this.appWindow.style.opacity = '0';
        }

        setTimeout(function() {
            /* 【隐藏而非销毁】保留DOM以便下次快速打开 */
            self.appWindow.classList.add('hidden');
            document.getElementById('desktop').style.filter = 'blur(0px)';

            /* 【恢复状态栏】 */
            var statusBar = document.querySelector('.status-bar');
            if (statusBar) {
                statusBar.classList.remove('status-bar-light', 'status-bar-dark', 'status-bar-transparent');
            }

            self.isCardMode = false;
            self.isCardDragging = false;

            /* 【重置样式但保留内容】 */
            self.appWindow.style.position = '';
            self.appWindow.style.left = '';
            self.appWindow.style.top = '';
            self.appWindow.style.width = '';
            self.appWindow.style.height = '';
            self.appWindow.style.transform = '';
            self.appWindow.style.opacity = '';
            self.appWindow.style.boxShadow = '';

            var homeIndicator = self.appWindow.querySelector('.home-indicator');
            var tabBar = self.appWindow.querySelector('.app-tab-bar');
            var appContent = self.appWindow.querySelector('.app-content-page');
            var homeIndicatorArea = self.appWindow.querySelector('.home-indicator-area');

            if (homeIndicator) homeIndicator.style.display = '';
            if (tabBar) tabBar.style.display = '';
            if (appContent) appContent.style.pointerEvents = '';
            if (homeIndicatorArea) homeIndicatorArea.style.display = '';

            self.appWindow.ontouchstart = null;
            self.appWindow.ontouchmove = null;
            self.appWindow.ontouchend = null;
            self.appWindow.onmousedown = null;
            self.appWindow.onclick = null;

        }, 300);
    };
```

### A.11 EnhancedApp 兼容性别名（3517-3518）

```javascript
    /* 【兼容性别名】保持向后兼容，EnhancedApp指向BaseApp */
    var EnhancedApp = BaseApp;
```

### A.12 PhoneCore.registerApp（9970-9986）

```javascript
        registerApp: function(app) {
            this.apps[app.id] = app;

            /* 【注册AI可见信息】
               如果App声明了aiVisibility配置，自动注册到全局注册表
               这样system.js就能动态发现所有App的AI可见信息配置 */
            if (app.aiVisibility && app.aiVisibility.enabled !== false) {
                this.aiVisibilityRegistry[app.id] = {
                    id: app.id,
                    name: app.aiVisibility.name || app.name,
                    icon: app.aiVisibility.icon || '',
                    desc: app.aiVisibility.desc || 'AI可读取' + (app.aiVisibility.name || app.name) + '数据',
                    dataSources: app.aiVisibility.dataSources || []
                };
                EventBus.emit('aiVisibility:registered', { appId: app.id, config: this.aiVisibilityRegistry[app.id] });
            }
        },
```

### A.13 PhoneCore.openApp（10003-10032）

```javascript
        /* 【打开指定App】
           通过appId打开对应的应用
           @param appId {string} 应用ID，如 'sms-app', 'chat-app' 等
           @param options {object} 可选参数，如 { animate: false } */
        openApp: function(appId, options) {
            var app = this.apps[appId];
            if (!app) {
                console.warn('[PhoneCore] App not found:', appId);
                return false;
            }

            // 调用应用的open方法
            if (typeof app.open === 'function') {
                // 确保应用窗口显示在最前面
                // 将窗口移动到 appContainer 的最后位置
                if (app.appWindow && app.appWindow.parentNode) {
                    var container = document.getElementById('appContainer');
                    if (container) {
                        container.appendChild(app.appWindow);
                    }
                }

                app.open();
                EventBus.emit('app:opened', { appId: appId, app: app });
                return true;
            }

            console.warn('[PhoneCore] App has no open method:', appId);
            return false;
        },
```

### A.14 全局导出（10189-10207）

```javascript
    // ============ 导出到全局 ============
    /* 全局导出 将核心类暴露到window对象，供其他脚本使用 */
    global.PhoneCore = PhoneCore;
    global.EventBus = EventBus;
    global.Database = Database;
    global.TimeSystem = TimeSystem;
    global.NotificationSystem = NotificationSystem;
    global.ResourceManager = ResourceManager;
    global.DesktopGrid = DesktopGrid;
    global.DesktopManager = DesktopManager;
    global.Widget = Widget;
    global.ShareCard = ShareCard;
    global.BadgeManager = BadgeManager;
    global.PromptFactory = PromptFactory;

    /* App基类统一的App构造函数，合并了原来的MyBaseApp和EnhancedApp */
    global.BaseApp = BaseApp;
    global.EnhancedApp = EnhancedApp; /* 兼容性别名 指向BaseApp */
    global.MyBaseApp = BaseApp; /* 兼容性别名 指向BaseApp，保持向后兼容 */
```

---

### 附录 B：代码段行号索引

| 代码段 | 行号范围 | 功能说明 |
|--------|----------|----------|
| A.1 | 2582-2645 | BaseApp 基类定义，窗口状态属性 |
| A.2 | 2784-2857 | 渲染 App 窗口，创建 Home Indicator |
| A.3 | 2885-2956 | 打开详情页 |
| A.4 | 3065-3134 | 详情页 Home 指示器事件绑定 |
| A.5 | 3136-3194 | 打开 App 动画 |
| A.6 | 3196-3268 | 主页面 Home 指示器事件绑定 |
| A.7 | 3270-3316 | 切换到卡片模式 |
| A.8 | 3318-3351 | 从卡片模式恢复 |
| A.9 | 3353-3452 | 卡片模式拖拽事件绑定 |
| A.10 | 3454-3515 | 退出 App，清理状态 |
| A.11 | 3517-3518 | EnhancedApp 兼容性别名 |
| A.12 | 9970-9986 | PhoneCore.registerApp |
| A.13 | 10003-10032 | PhoneCore.openApp |
| A.14 | 10189-10207 | 全局导出 |

    /* 【绑定图标点击事件】
       解决图标点击需要两次的问题：
       1. 使用addEventListener确保可靠绑定
