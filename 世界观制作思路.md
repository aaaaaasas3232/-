# 世界观制作思路

> 世界观是 AI 活动的背景舞台，它管理**空间、时间、资产**三大实体模块。

---

## 更新日志

| 版本 | 说明 |
|------|------|
| **v2.0** | **本版：完全基于真实代码重写**。清理所有 `toolkit.world` 为 `settingsSdk`；对齐实际 SDK API（`settingsSdk.places`/`locations`/`chronology`/`timelines`/`anchors`）；更新文件结构；移除 `toolkit.social`/`toolkit.persona` 等不存在的接口。 |

*最后更新：2026-07-30 v2.0*

---

## 0. 核心定位

**世界观（World）** = AI 角色存在的舞台 + AI 上下文的来源。

### 0.1 四大模块结构

```
┌─────────────────────────────────────────────────────────────────────┐
│                       世界观四大模块                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   ┌─────────────────────────┐    ┌─────────────────────────┐       │
│   │   🗺️ 空间(Space)        │    │   ⏰ 时间(Time)          │       │
│   │                         │    │                         │       │
│   │   • 地点(Place)        │    │   • 纪时系统            │       │
│   │     - 城市/区域        │    │     - 时间单位映射       │       │
│   │   • 场所(Location)      │    │     - 现实↔世界 转换  │       │
│   │     - 地点下的 pin     │    │   • 时间轴 / 锚点      │       │
│   └─────────────────────────┘    └─────────────────────────┘       │
│                                                                     │
│   ┌─────────────────────────┐    ┌─────────────────────────┐       │
│   │   💎 资产(Assets)        │    │   📎 夹子(Clip)          │       │
│   │                         │    │                         │       │
│   │   • 资产类型定义         │    │   • 碎知识 prompt 库     │       │
│   │   • 单位/初始值         │    │   • 预设模板             │       │
│   │   • 商城/物价锚点       │    │                         │       │
│   └─────────────────────────┘    └─────────────────────────┘       │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 0.2 ★ 重要决策

- **阶段系统已下放到人设侧**。世界观不再管"人生阶段 / 关系阶段 / 阶段转折"。人物的人生轨迹、剧情推进都在人设里管理。
- **时间相关的事件数据**仍属于世界观（时间轴/时间表/时间锚点），但"谁在哪年做了什么"这种"个人时间线"由人设自己写。
- **资产类型定义**属于世界观；**具体某个人持有多少**属于人设。
- **空间定义**（地点/场所）属于世界观；**某人在某阶段能不能去**的逻辑由人设侧根据世界观提供的空间做选择。

---

## 1. 世界观组 (WorldGroup)

用于组织和管理多个世界观，本身不携带业务内容。

```javascript
{
    id: 'group-modern',
    name: '现代世界观',
    icon: '🏙️',
    color: '#3b82f6',
    order: 0,
    createdAt: Date.now(),
    updatedAt: Date.now()
}
```

---

## 2. 世界观数据结构（完整字段）

```javascript
{
    // ===== 基础 =====
    id: 'world-student-life',
    groupRef: 'group-modern',
    name: '学生日常生活',
    version: '1.0.0',
    changelog: [],
    summary: '现代校园生活世界观',
    keyPoints: [
        '高中三年为主线',
        '电竞作为副线',
    ],

    // ===== ★ 空间 =====
    places: [],           // 地点列表（见 §3）
    locations: [],        // 场所列表（见 §3）

    // ===== ★ 时间 =====
    chronologySettings: {}, // 纪时配置（见 §4）
    timelines: [],          // 事件列表（见 §5）
    anchors: [],           // 时间锚点（见 §5）

    // ===== ★ 资产 =====
    assets: [],            // 资产类型定义（见 §6）

    // ===== 元数据 =====
    createdAt: Date.now(),
    updatedAt: Date.now()
}
```

---

## 3. ★ 空间（Space）— 地点 + 场所

### 3.1 设计理念

空间是 AI 与用户"能去哪里"的物理/地理上下文。分两层：

```
┌────────────────────────────────────────────────────────────────┐
│   空间 = 地点(Place) + 场所(Location)                          │
│                                                                │
│   地点 — 大尺度区域，通常是一座城市/一片区域                    │
│   ┌─────────────────────────────────────────┐                  │
│   │  例：A 城、B 城、北方大陆              │                  │
│   │  特点：有自己的坐标、距离其他地点的数值  │                  │
│   └─────────────────────────────────────────┘                  │
│             │                                                  │
│             ▼                                                  │
│   场所 — 地点内部的具体 pin                                    │
│   ┌─────────────────────────────────────────┐                  │
│   │  例：医院、学校、酒馆                  │                  │
│   │  归属：必须挂在某个地点下              │                  │
│   │  坐标：相对所属地点的相对坐标          │                  │
│   └─────────────────────────────────────────┘                  │
└────────────────────────────────────────────────────────────────┘
```

### 3.2 地点 (Place)

```javascript
{
    id: 'place-a-city',
    worldRef: 'world-student-life',
    name: 'A 城',
    isCenter: true,                  // ★ 主地点：必须有一个，作为距离基准
    position: { x: 50, y: 50 },   // 地图坐标（0~100 的相对值）

    distanceFromCenter: 0,
    distanceUnit: '公里',

    tags: ['一线城市', '主场'],
    description: '玩家所在的城市，繁华的现代都市',

    icon: '🏙️',
    color: '#3b82f6',

    createdAt: Date.now(),
    updatedAt: Date.now()
}
```

### 3.3 场所 (Location)

```javascript
{
    id: 'loc-hospital',
    worldRef: 'world-student-life',
    placeRef: 'place-a-city',       // ★ 归属地点
    name: 'A 城中心医院',

    relativePosition: { x: 30, y: 40 },

    tags: ['医疗', '公共场所'],
    description: 'A 城最大的公立医院',
    icon: '🏥',

    createdAt: Date.now(),
    updatedAt: Date.now()
}
```

---

## 4. ★ 时间（Time）— 纪时系统

### 4.1 设计理念

不同世界观的时间单位与流速不同（古代十二时辰 vs 现代二十四小时）。

纪时系统让 AI 理解「世界观时间」与「现实时间」的对应关系。

### 4.2 时间层级

```
大周期(年/纪/元) → 中周期(月/章/旬) → 小周期(日/天) → 微周期(时/时辰)
```

### 4.3 纪时配置字段

```javascript
{
    chronologySettings: {
        enabled: true,
        baseYear: 2000,                   // 基准年

        yearLabel: '纪',
        monthLabel: '章',
        dayLabel: '日',
        hourLabel: '时辰',

        yearRatio: 1,                      // 世界:现实 = 1:1
        monthRatio: 1,
        dayRatio: 1,

        customHours: [                     // 可选，12 时辰制
            '子时', '丑时', '寅时', '卯时', '辰时', '巳时',
            '午时', '未时', '申时', '酉时', '戌时', '亥时'
        ],
    }
}
```

---

## 5. ★ 资产（Assets）

### 5.1 设计理念

资产是「世界里流通的物品/价值度量」。**世界观定义资产类型**，**人设持有具体数值**。

```
世界观(类型定义)                人设(具体数值 · v4.1)
  ├── 灵石(单位:枚)    ──────►  assetBalance + incomeEvents[]
  └── 人气值(单位:点) ──────►  单货币聚合 + 任意定时收入
```

### 5.2 资产类型结构

```javascript
{
    id: 'asset-money',
    worldRef: 'world-student-life',
    name: '金币',
    icon: '💎',
    unit: '枚',
    defaultValue: 1000,
    category: 'currency',               // 'currency' | 'item' | 'status' | 'custom'
    description: '通用货币',

    isTransferable: true,
    isConsumable: false,
    maxValue: null,
}
```

---

## 6. ★ 夹子（Clip）

夹子是一个**容器模块**，装碎知识 prompt 库。

### 6.1 碎知识 Prompt 库

```javascript
{
    id: 'clip-k-001',
    worldRef: 'world-student-life',
    name: '爱意棒',
    description: '这个世界观里人们以此为食，吃下去会产生强烈的幸福感',
    tags: ['食物', '文化'],
}
```

---

## 7. SDK 接口汇总

> **重要**：所有世界观相关 API 通过 `window.settingsSdk` 访问。**不存在 `toolkit.world`**。

### 7.1 世界观基础

```js
// settingsSdk.worlds（通用 CRUD）
settingsSdk.worlds.list()
settingsSdk.worlds.get(id)
settingsSdk.worlds.create({ name, groupRef })
settingsSdk.worlds.update(id, patch)
settingsSdk.worlds.remove(id)
settingsSdk.worlds.getActive()
settingsSdk.worlds.setActive(id)

// settingsSdk.worldGroups
settingsSdk.worldGroups.list()
settingsSdk.worldGroups.create({ name, icon, color })
settingsSdk.worldGroups.update(id, patch)
settingsSdk.worldGroups.remove(id)
```

### 7.2 空间（地点 + 场所）

```js
// settingsSdk.places（地点）
settingsSdk.places.list({ worldRef })
settingsSdk.places.get(id)
settingsSdk.places.create({ worldRef, name, isCenter, position })
settingsSdk.places.update(id, patch)
settingsSdk.places.remove(id)

// settingsSdk.locations（场所）
settingsSdk.locations.list({ worldRef })
settingsSdk.locations.get(id)
settingsSdk.locations.create({ worldRef, placeRef, name, relativePosition })
settingsSdk.locations.update(id, patch)
settingsSdk.locations.remove(id)
```

### 7.3 纪时系统

```js
settingsSdk.chronology.getConfig(worldId)
settingsSdk.chronology.update(worldId, settings)
settingsSdk.chronology.realToWorld(realDate, worldId)
settingsSdk.chronology.worldToReal(worldTime, worldId)
settingsSdk.chronology.format(timestamp, 'full'|'date'|'time'|'verbose', worldId)
settingsSdk.chronology.getHourNames(worldId)
settingsSdk.chronology.getHourName(hour, worldId)
```

### 7.4 时间线 / 锚点

```js
// settingsSdk.timelines
settingsSdk.timelines.list({ worldRef, ownerId, category })
settingsSdk.timelines.get(id)
settingsSdk.timelines.create({ worldRef, title, date, category, ownerId })
settingsSdk.timelines.update(id, patch)
settingsSdk.timelines.remove(id)
settingsSdk.timelines.addEvent(worldId, data)

// settingsSdk.anchors
settingsSdk.anchors.list({ worldRef, type })
settingsSdk.anchors.create({ worldRef, type, scope, name, startDate, endDate })
settingsSdk.anchors.update(id, patch)
settingsSdk.anchors.remove(id)
```

### 7.5 资产

```js
// settingsSdk.worlds 的 CRUD 已覆盖
// 资产类型：settingsSdk.worlds.update(id, { assets: [...] })
```

### 7.6 标签

```js
// settingsSdk.tagGroups
settingsSdk.tagGroups.list()
settingsSdk.tagGroups.create({ name, color })
settingsSdk.tagGroups.update(id, patch)
settingsSdk.tagGroups.remove(id)

// settingsSdk.tags
settingsSdk.tags.list({ groupRef })
settingsSdk.tags.create({ groupRef, name, color })
settingsSdk.tags.update(id, patch)
settingsSdk.tags.remove(id)
```

### 7.7 快照 / 加权抽卡

```js
settingsSdk.snapshot.get(worldId)
settingsSdk.snapshot.set(worldId, data)
// weightedPick 是顶层函数
settingsSdk.weightedPick(items, weights, n)
```

### 7.8 日记 / 日程（人设侧）

```js
// settingsSdk.diary
settingsSdk.diary.getToday(entityType, entityId)
settingsSdk.diary.get(entityType, entityId, date)
settingsSdk.diary.upsert(record)
settingsSdk.diary.addSegment(entityType, entityId, date, { text, source })
settingsSdk.diary.removeSegment(entityType, entityId, date, segmentId)

// settingsSdk.schedule
settingsSdk.schedule.getForEntity(entityType, entityId)
settingsSdk.schedule.updateForEntity(entityType, entityId, patch)
```

---

## 8. UI 结构

```
世界观设置页
├── 左侧:世界观列表(按组分组)
│     ├── 组 A(现代)
│     │   ├── 学生日常生活
│     │   └── 都市职场
│     └── 组 B(奇幻)
│         └── 修仙世界
│
└── 右侧:当前选中世界观的详情
      ├── 基础设定(名字/简介/keyPoints)
      ├── ★ 空间 Tab
      │     ├── 地点列表(可视地图)
      │     └── 选中地点后:场所列表
      ├── ★ 时间 Tab
      │     ├── 纪时系统配置
      │     └── 时间轴/锚点
      ├── ★ 资产 Tab
      │     └── 资产类型列表
      └── 元数据(版本/更新日志)
```

---

## 9. 文件结构

```
js/apps/setting/world/
├── bootstrap.js              # 启动初始化
├── library.js              # 世界观列表/切换逻辑
├── methods.js              # 业务方法
├── index.js               # 注册入口
└── sdk/                   # ★ settingsSdk 实现
    ├── settings-sdk.js     # 总入口（createSettingsSdk）
    ├── crud.js             # 通用 CRUD 工厂
    ├── defaults.js
    ├── profile-schema.js
    ├── form-schema.js
    ├── form-reader.js
    ├── form-renderer.js
    ├── helpers.js
    ├── bus.js              # 事件总线
    ├── groups.js           # 世界观组
    ├── tags.js            # 标签组/标签
    ├── snapshots.js       # 每日快照
    ├── profile.js         # profile 模式
    ├── timelines.js       # 时间线
    ├── drafts.js          # 草稿
    ├── anchors.js         # 时间锚点
    ├── diary.js           # 日记 SDK
    ├── schedule.js         # 日程 SDK
    ├── persona.js         # 人设 SDK
    └── geo/               # ★ 空间（地点 + 场所）
        ├── index.js
        ├── places.js      # 地点 API
        └── locations.js   # 场所 API
```

---

## 10. 与人设的关系

### 10.1 世界观 → 人设

```
世界观 ─────────► 人设(引用 ID,不复制数据)

地点/场所 ───────► locations[] 引用 placeId/locationId
纪时系统 ───────► experience[] 引用纪时单位
资产类型 ───────► assetBalance + incomeEvents[] 引用世界基准货币
碎知识 ─────────► knowledgeRefs 引用 knowledgeId
```

### 10.2 人设侧读取世界

```js
// 人设侧通过 settingsSdk 读世界观
const places = settingsSdk.places.list({ worldRef: persona.boundWorldId });
const locations = settingsSdk.locations.list({ worldRef: persona.boundWorldId });
const chrono = settingsSdk.chronology.getConfig(persona.boundWorldId);
```

---

## 11. 与各模块的关系

| App | 从世界观拉取 | 写入世界观 |
|-----|------------|-----------|
| 人设 | 空间/资产/时间/碎知识 | 个人事件到 timelines |
| 商城 | 资产单位 + 基准货币类型 | 无 |
| 日记 | 当前世界时间 | 可写入 timelines |
| 聊天 | 碎知识当 AI 上下文 | 可写入 timelines |

---

*最后更新：2026-07-30 v2.0（清理 SDK 接口，统一为 settingsSdk）*
